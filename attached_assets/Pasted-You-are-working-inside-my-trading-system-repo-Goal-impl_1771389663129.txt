You are working inside my trading system repo. Goal: implement cross-asset correlation tracking + smart correlation blocking for v5 walk-forward / forward-test.

Context:
- I trade multiple symbols (BTCUSDT, ETHUSDT, SOLUSDT) in the same run.
- I previously used --no-correlation-block (disables any blocking) but I want a smarter blocker instead.
- The issue: BTC/ETH/SOL are often highly correlated. When multiple symbols fire same-direction trades simultaneously, drawdowns cluster and weekly loss caps get hit fast.
- Requirement: add diagnostics + gating.

Deliverables (MUST DO ALL):
1) Cross-asset correlation tracking (per walk-forward fold):
   - Compute a DAILY return series in R-units per symbol (daily sum of realized trade R).
   - Build a correlation matrix (Pearson) across symbols using aligned daily R series.
   - Also compute “overlap ratio”: fraction of bars where >1 position is open simultaneously across symbols.
   - Log these to console with a clear section header: [V5_CORR] Fold <k> correlation matrix + overlap ratio.
   - Save to JSON alongside forward report output (e.g. checkpoints/v5_corr_report_fold_<k>.json). Include:
        { fold_id, window_train, window_test, symbols, daily_r_series (compact), corr_matrix, mean_abs_corr, max_abs_corr_pair, overlap_ratio }

2) Smart correlation blocking (replaces blunt --no-correlation-block):
   - Add CLI flags (default ON):
        --v5-corr-block (bool, default true)
        --v5-corr-window-days (int, default 30)
        --v5-corr-thresh (float, default 0.70)
        --v5-corr-same-side-only (bool, default true)
        --v5-log-corr-matrix (bool, default true)
   - The blocker should prevent opening a new trade on symbol S if:
        a) Another symbol A already has an OPEN position (not pending)
        b) abs(rolling_corr(S,A)) >= thresh
        c) and (if same-side-only) the new trade side matches the open position side
   - Rolling correlation should be computed from the last N daily R values (window-days) for each symbol within the current fold.
   - If correlation data is insufficient (less than 10 aligned days), DO NOT block (return allow).
   - Log every blocked decision with: [V5_CORR_BLOCK] blocked <sym> side=<+1/-1> due to corr(<sym>,<other>)=<val> >= <thresh>

3) Integration points:
   - The correlation blocker must run in the ENTRY decision path (right before creating a position/order).
   - It must work with existing limits: --max-concurrent, --max-pos-total, --max-pos-symbol, risk caps, cooldown, weekly loss cap.
   - It must NOT break single-symbol runs (gracefully disabled if only 1 symbol).
   - Keep backward compatibility: if --no-correlation-block exists, keep it but mark as deprecated in help text; if set, it should override and disable corr blocking entirely.

4) Testing:
   - Add unit tests for:
        a) correlation matrix computation correctness with synthetic daily series
        b) blocker triggers when corr is high and same-side open position exists
        c) blocker does not trigger with insufficient data
        d) blocker does not trigger when sides differ and same-side-only=true
   - Tests must run without network or torch.

5) Code quality:
   - Keep changes minimal and localized.
   - Add a small helper module (e.g. v5/correlation.py) with RollingDailyCorr class + utilities.
   - Ensure logs and JSON are deterministic.

Steps to execute:
A) First, locate where --no-correlation-block is parsed and where the current correlation blocking logic (if any) exists.
B) Locate the entry decision path where trades are opened. Insert the new gating check there.
C) Implement daily-R aggregation:
   - Add a per-symbol dict[date]->R that updates when trades CLOSE.
D) At fold end, compute and log correlation matrix + overlap, and write JSON.
E) Add tests.

Output:
- Make the code changes.
- Show a summary of which files were changed and what was added.
- Provide examples of the new logs and JSON format.
- Confirm the new flags appear in --help and default behavior is corr blocking ON.