You are working in the existing gpu_trainer + web dashboard repo. Goal: upgrade the current “Live System” dashboard into a PRO-grade, fully async, realtime, AI-powered analytics dashboard.

NON-NEGOTIABLE REQUIREMENTS
1) Everything must be realtime and async:
   - Use WebSockets (preferred) or Server-Sent Events for streaming live cycle logs, signals, position updates, and training/learning progress.
   - The UI must update without page refresh (live tables and charts).

2) Persistent storage (must not be in-memory):
   - All cycles, signals, trades, fills, and learning runs MUST be stored in a DB (SQLite ok for MVP, Postgres-ready schema).
   - Implement migrations (Prisma or Drizzle recommended).
   - Must support querying historical ranges (last 24h, 7d, 30d).

3) Advanced dashboard sections (must implement):
   A) Live Overview (already exists but make richer)
      - Open positions, closed trades, win rate, total net R
      - Today stats: trades today, net R today, avg R/trade, best/worst trade, max drawdown (paper/live separately)
      - Health lights: Data OK, HTF OK, Model loaded, Websocket connected, Last update age

   B) Realtime “Cycle Monitor”
      - For each 15m cycle: per symbol row with:
        price, p_enter, HTF alignment, direction, gate reason, decision (ENTER/HOLD/COOLDOWN), policy params used, model version
      - Show last N cycles (configurable)
      - Must show counts per cycle: candidates, gated out, below threshold, blocked by portfolio, blocked by correlation, skipped by execution

   C) Trade Journal (must be very detailed)
      - Full trade timeline: created_at, entered_at, exited_at, bars held
      - Entry/SL/TP, side, symbol, size, leverage, fees, spread, slippage, gross_r, cost_r, net_r
      - Outcome: TP / SL / EXPIRE / MANUAL
      - Add a “Replay” drawer: show 15m candle path during the trade, mark entry/exit, and show which gates were true at entry
      - Filters: symbol, date range, side, outcome, model version, policy, threshold ranges
      - Export CSV

   D) Learning / Training Panel (AI-looking, very clear)
      - Show “data ingestion” progress: new bars per symbol per day, total bars, missing bars, NaN rate
      - Show “label stats”: pos rate, regime distribution, triple-barrier counts (TP/SL/EX)
      - Show training runs history: start/end time, dataset window, epochs, best val loss, PR-AUC, selected checkpoint, promotion decision
      - Show safety-gate results: PF_net, E_net, profitable regimes, pass/fail reasons
      - Must display “how much the system learned” each run:
        * #new bars added since last train
        * #new trades since last train
        * parameter delta summary (e.g., L2 norm of weights change) OR model hash changed + metrics diff
      - Show current active model: version, trained_on window, last promoted time

   E) Performance & Risk analytics
      - Equity curve in R-units (net)
      - Rolling PF_net, rolling E_net
      - Drawdown chart
      - Per-symbol breakdown (PF_net, E_net, win rate, trades/day)
      - Regime breakdown (2021, 2022, 2023-2024 like your eval)
      - Distribution charts: net_r histogram, holding time histogram, p_enter vs outcome calibration plot
      - Execution analytics: pullback entries attempted, succeeded, skipped, avg improvement bps, market fallback count

   F) “AI Insights” panel (do NOT fake)
      - Generate insights deterministically from stored data (no hallucination):
        * “Top 3 reasons for HOLD in last 24h”
        * “Which symbol has best net expectancy this week”
        * “Are losses clustered by time-of-day or regime”
        * “Is p_enter calibration drifting (rolling reliability)”
      - These are computed summaries, not LLM text. You can label it “AI Insights” but it must be data-driven.

4) Visual/UX requirements (make it premium)
   - Dark, modern, high-contrast, subtle gradients, glassmorphism cards, smooth animations
   - Skeleton loading + “last updated” timestamps everywhere
   - Responsive layout (desktop first, tablet ok)
   - Use a chart library (Recharts, ECharts, or Chart.js) for all graphs

5) API + Events (must implement)
   - REST endpoints for historical queries:
     /api/cycles?from&to&symbol
     /api/trades?from&to&symbol&outcome&model_version
     /api/learning-runs?from&to
     /api/perf/summary?window=24h|7d|30d
   - Realtime stream:
     /ws stream that emits event types:
       CYCLE_UPDATE, SIGNAL_UPDATE, TRADE_OPEN, TRADE_UPDATE, TRADE_CLOSE, LEARNING_PROGRESS, MODEL_PROMOTED, HEALTH_STATUS

6) Integration with local GPU runner
   - Add a small “ingestion” module on the server that accepts POST events from local runner:
     POST /ingest/event {type, payload, ts}
   - Local runner can push cycle updates and trade updates; server stores them, re-broadcasts via WS to all clients.
   - MUST validate payload shape with zod (or similar) and reject invalid events.
   - Must dedupe events by (event_id) to avoid duplicates when network retries happen.

7) Data model (must implement these tables)
   - cycles(id, ts, timeframe, symbol, price, p_enter, direction, htf_aligned, gate_reason, decision, model_version, policy_json, features_meta_json)
   - trades(id, symbol, side, created_at, entry_at, exit_at, entry, sl, tp, size, leverage, fees_bps_entry, fees_bps_exit, spread_bps, slip_k, gross_r, cost_r, net_r, outcome, model_version, policy_json, notes)
   - trade_events(id, trade_id, ts, type, payload_json)  // for timeline/replay
   - learning_runs(id, start_at, end_at, data_from, data_to, new_bars, new_trades, pr_auc, val_loss, pf_net, e_net, profitable_regimes, promoted, reason, model_version, metrics_json)
   - health(id, ts, component, status, message)

8) Quality gates
   - NO fake numbers. If data missing, show “N/A” and log why.
   - Must show “paper vs live” clearly everywhere.
   - Must support multi-asset. No BTC-only assumptions.

DELIVERABLES
- Implement backend storage + migrations
- Implement ingestion endpoint + WS broadcasting
- Implement frontend pages/tabs: Overview, Live, Trades, Learning, Performance, Diagnostics, AI Insights
- Add at least 8 charts: equity curve, drawdown, rolling PF, rolling E, per-symbol bar chart, net_r histogram, p_enter calibration, holding time hist
- Add CSV export for trades
- Update README with how to run:
  - Start web
  - Start ws server
  - Configure local runner to POST to /ingest/event and open /ws

VERIFICATION (must print these logs)
- On server start: “DB connected”, “WS ready”
- On ingest: “event accepted type=...”, “broadcast to N clients”
- On UI: “WS connected”, “last update Xs ago”
- Create a demo seed script that inserts sample events so UI can be tested without GPU runner.

Start by scanning existing codebase:
- Identify current dashboard stack (Next.js/React?) and current API patterns.
- Implement minimal DB + ingest + WS first (end-to-end).
- Then build UI pages incrementally, reusing existing styling but upgrading it to premium.

Do NOT:
- change ML code
- change trading logic
- fabricate metrics

Just build the most advanced realtime dashboard around the existing system.
