TASK: Implement v4.5.2 “Directional Separation Fix” to stop PR-AUC collapse (mean_logit_neg > mean_logit_pos) WITHOUT changing labels, horizon, data pipeline, model structure, OHEM, or edge/value heads.

GOAL:
- Ensure logits for positives are higher than negatives on VAL.
- Add a small margin separation loss + flip penalty.
- Reduce focal alpha (too aggressive on positives).

CHANGES (DO EXACTLY THIS):

1) In the ENTER QUALITY training loop where you compute total_loss for the binary enter_head logits:
- Keep focal BCE, OHEM, edge_head, value_head as-is.
- After you compute classification logits (enter logits) and already have y (0/1), add this block:

    # === v4.5.2 Directional Separation Fix ===
    pos_mask = (y == 1)
    neg_mask = (y == 0)
    if pos_mask.any() and neg_mask.any():
        mean_pos = logits[pos_mask].mean()
        mean_neg = logits[neg_mask].mean()
        sep = mean_pos - mean_neg
        sep_loss = torch.relu(0.30 - sep)          # require min margin
        flip_penalty = torch.relu(mean_neg - mean_pos)  # extra penalty if reversed
        total_loss = total_loss + 0.03 * sep_loss + 0.05 * flip_penalty

- IMPORTANT: apply ONLY to the classification logits for enter_head. Do NOT apply to value_head or edge_head.

2) Reduce focal alpha:
- Find focal_bce_with_logits call or config where alpha=0.60 and change to alpha=0.35.
- Keep gamma=1.5.

3) Add validation logging each epoch:
- After validation forward pass (logits_val, y_val), print:

    [SEP_CHECK] mean_pos=... mean_neg=... sep=...

Use:

    pos_mask = (y_val == 1)
    neg_mask = (y_val == 0)
    if pos_mask.any() and neg_mask.any():
        mean_logit_pos = logits_val[pos_mask].mean().item()
        mean_logit_neg = logits_val[neg_mask].mean().item()
        sep = mean_logit_pos - mean_logit_neg
        print(f"[SEP_CHECK] mean_pos={mean_logit_pos:.3f} mean_neg={mean_logit_neg:.3f} sep={sep:.3f}")

4) Version bump:
- Update version string to: v4.5.2_directional_sep_fix

VERIFICATION (must paste logs):
- Run training for 15 epochs and paste:
  - PR-AUC line
  - Pred% line
  - [SEP_CHECK] line (sep should be positive)
- Success criteria:
  - sep > 0 on VAL by epoch <= 10
  - Pred% not stuck at 0% or 100%
  - PR-AUC improves from the ~0.09 collapse trend

DO NOT TOUCH ANYTHING ELSE.