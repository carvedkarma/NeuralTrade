✅ Replit Agent Prompt (Detailed) — v3.4.0 “Geometry Extraction + Net Profit After Costs”

You are working in the gpu_trainer project. Current system status:

Model: ENTER Quality binary head (enter_head) only; other heads disabled during training.

Labels: HTF trend alignment + triple-barrier parity (triple_barrier.py is the single source of truth).

Features: 63 columns (47 STF + 10 HTF + 3 Funding + 3 OI).

Costs: Implemented in compute_trade_cost_r() in triple_barrier.py (fees + spread + ATR-based slippage), converted to R-units, and applied as net_r = gross_r - cost_r.

Sizing: confidence sizing exists but do not tune it yet (it made results worse when net edge is negative).

Regime Eval: --regime-eval runs single-row inference (batch=1, features=63) matching live inference.

Problem we must solve

Gross profitability exists (PF_gross ~ 1.27), but PF_net < 1 with realistic costs. This means the edge is being eaten by trade geometry (TP/SL too tight), not by ML.

Objective (v3.4.0)

Make PF_net > 1.05 and E[net] > 0 while keeping trade frequency near 3–5 trades/day, without changing the model or adding features.

✅ Required Version Update

Bump system version to:
v3.4.0_geometry_sweep_net_profit

Update version string wherever feature/model versions are tracked (same pattern used previously: e.g., v3.3.1, v3.3.0).

✅ Task 1 — Add Geometry Sweep to Regime Eval (core work)

Add CLI args (default to current behavior):

--tp-mults (comma list) default: "2.0"

--sl-mults (comma list) default: "1.5"

--thresholds (comma list) default: "0.70"

--cooldowns (comma list) default: "4"

--size-cap remains supported but DO NOT tune it; keep default 2.0

Costs must remain supported:

--fees-entry-bps default 5.0

--fees-exit-bps default 5.0

--spread-bps default 1.0

--slip-k default 0.10

Implement a sweep loop in --regime-eval so it can evaluate multiple configurations in one run:

For each (tp_mult, sl_mult, threshold, cooldown) combination:

Run the same policy across regimes using:

the same inference (single-row)

the same HTF gates

the same triple-barrier outcomes/parity logic

the same cost model (net R)

Print a compact per-config summary + per-regime metrics.

✅ Task 2 — Hard Selection Rule (must be automatic)

For each tested config, compute:

Overall PF_net

Overall E[net]

Overall trades/day

Avg WinR_net and Avg LossR_net

Count of regimes where PF_net > 1

Pick “BEST” config using this exact priority order:

Prefer configs where:

Overall PF_net >= 1.05

Overall E[net] > 0

Trades/day between 3.0 and 5.0

At least 2 regimes profitable (PF_net > 1)

If multiple pass, choose highest PF_net.

If none pass, choose the config with:

highest PF_net

and trades/day closest to 4.0

Mark the selected config clearly in logs as:
<< BEST CONFIG (v3.4.0) >>

✅ Task 3 — Default Sweep Set (the exact tests to run)

Hardcode a “recommended sweep preset” when user passes --geometry-sweep:

TP/SL combos:

TP=2.5, SL=1.25

TP=3.0, SL=1.5

TP=3.5, SL=1.5

Thresholds:

0.70, 0.75

Cooldowns:

4, 6

Costs (defaults must remain taker-realistic):

entry=5 bps, exit=5 bps

spread=1 bp

slip_k=0.10

This produces 3 × 2 × 2 = 12 runs in one command.

✅ Output Format Requirement

Keep the existing regime table but add a one-line summary per config:

Example:
CFG tp=3.0 sl=1.5 thr=0.75 cd=6 | PF_n=1.11 | E_n=+0.04 | TPD=3.9 | WinR_n=+1.90 | LosR_n=-1.05 | profitable_regimes=3/3 << BEST

Also print:

Avg CostR

Avg SzMul (even if sizing is fixed; this helps later)

✅ Do NOT Do (strict)

Do NOT add new features (NO new OI/funding/sentiment)

Do NOT change the model architecture or heads

Do NOT tune focal loss / PR-AUC

Do NOT change triple-barrier rules (parity must remain)

Do NOT change inference windowing (must stay single-row)

We are optimizing trade extraction, not ML.

✅ How I will run it

I will run this command:

python quick_start.py --url https://APP.replit.app \
  --regime-eval --geometry-sweep \
  --fees-entry-bps 5 --fees-exit-bps 5 --spread-bps 1 --slip-k 0.10


And you must output:

Per-config summary

Per-regime table

Best config selection

✅ Success Criteria (stop condition)

We are done when:

PF_net >= 1.05 overall

E[net] > 0 overall

Trades/day ≈ 3–5

≥2 regimes profitable after costs

Once achieved:

Print the final chosen policy parameters (tp_mult, sl_mult, threshold, cooldown, costs)

Save them to a small JSON config file:

checkpoints/best_policy_v3.4.0.json