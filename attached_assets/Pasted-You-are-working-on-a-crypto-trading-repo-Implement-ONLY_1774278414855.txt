You are working on a crypto trading repo. Implement ONLY the tasks below, in order, with minimal unnecessary refactoring. The objective is to make the system materially more correct and usable tonight. Do not invent new strategy ideas. Fix the verified issues.

IMPORTANT RULES
- Do not rewrite scoring math unless current tests prove it is wrong.
- Preserve existing CLI behavior unless absolutely necessary.
- Keep changes backward-compatible where practical.
- Add clear diagnostics/logging for every degraded mode or safety fallback.
- After each task, provide before/after evidence from code, logs, or metrics.
- If a task is too large, still complete the highest-value core of it instead of skipping.

PRIORITY ORDER
1. Fix adaptive sizing so trades are not pinned to the floor
2. Add shared train/live config
3. Make optional imports safe
4. Add symbol kill recovery path
5. Replace hard correlation block with a reliability-aware size penalty
6. Make live logs truthful
7. Add minimal live pre-trade halt switches

==================================================
TASK 1 — FIX ADAPTIVE SIZING
==================================================

Problem:
The current Kelly sizing is mathematically pinned to the minimum for the model’s normal win rates. As a result, avg_size_mult sits at the floor with near-zero variance, so edge cannot express itself.

What to do:
- Trace the full sizing path from model outputs to final size multiplier.
- Instrument and log these intermediates:
  - p_win
  - b / payoff ratio
  - raw_kelly_f
  - kelly_after_fraction
  - soft_gate_mult
  - pre_floor_size_mult
  - final_size_mult
- Keep the existing architecture shape, but change the sizing math so that realistic model win rates can produce size multipliers above the minimum floor.

Implementation guidance:
- Do NOT use raw Kelly fraction as the direct live size multiplier if it is structurally too small for the strategy domain.
- Convert Kelly output into a bounded confidence/risk scalar rather than directly mapping tiny Kelly values to size.
- Keep hard caps and safety floors.
- Preserve regime, drawdown, and other safety multipliers, but ensure they do not mathematically guarantee floor-only sizing.
- Ensure higher-conviction trades can size above floor.
- Keep behavior conservative.

Acceptance criteria:
- Fold summaries show size multiplier standard deviation > 0
- At least some high-conviction trades size above the current floor
- Add a diagnostic summary per fold showing min/mean/max/std of final_size_mult
- Document the exact old vs new formula in comments

==================================================
TASK 2 — ADD SHARED TRAIN/LIVE CONFIG
==================================================

Problem:
Train and live use disconnected defaults, causing parity failures.

What to do:
- Add a new shared config module, e.g.:
  config/shared_v5_trade_config.py
- Create a dataclass or equivalent central config for shared trading parameters.

Include at least:
- score_threshold
- score_lambda
- min_mu_r_score
- min_p_side
- min_p_short
- slippage_base_bps / cost_bps
- corr_thresh
- cooldown
- symbol kill threshold
- sizing parameters
- any core side-mode flags used by both train and live

Then:
- Update training code to load these defaults from the shared config
- Update live_runner.py to load the same defaults
- Preserve CLI/env overrides
- Remove silent default mismatches

Acceptance criteria:
- Print a startup config summary in both train and live paths
- Demonstrate that train and live read the same default values when no overrides are passed
- Keep current external interfaces working

==================================================
TASK 3 — MAKE OPTIONAL IMPORTS SAFE
==================================================

Problem:
Missing optional libraries currently crash startup.

What to do:
- In data/pipeline.py:
  - wrap pywt import in try/except ImportError
  - add HAVE_PYWT flag
  - if unavailable, disable wavelet feature generation cleanly
  - keep behavior shape-stable if downstream expects fixed columns
  - log clearly that wavelet features are disabled

- In models/__init__.py:
  - wrap sentiment-related imports in try/except ImportError
  - do not crash core model imports if transformers is unavailable
  - expose only available symbols cleanly
  - log/debug-note that sentiment components are unavailable

Acceptance criteria:
- Repo starts without crashing when pywt is absent
- Repo starts without crashing when transformers is absent
- Degraded mode is explicit in logs, not silent

==================================================
TASK 4 — ADD SYMBOL KILL RECOVERY PATH
==================================================

Problem:
Once a symbol is killed inside a fold, it stays dead permanently even if it later recovers strongly.

What to do:
- Find the symbol kill logic and add a recovery/re-enable mechanism.
- Keep the kill logic configurable from the shared config.
- Add explicit recovery rules, for example based on one or more of:
  - cooldown period after kill
  - rolling expectancy recovery
  - cumulative R recovery
  - minimum trade count after re-enable checkpoint
- Keep the behavior conservative and deterministic.

Implementation requirements:
- Log every symbol kill with:
  - symbol
  - timestamp/fold context
  - trigger metric
  - threshold
- Log every symbol re-enable with:
  - reason
  - recovery metric(s)
- Avoid flip-flopping rapidly; add hysteresis/cooldown if needed.

Acceptance criteria:
- A killed symbol can be re-enabled under explicit rules
- Kill/re-enable events appear in logs and summaries
- Thresholds and recovery rules are configurable

==================================================
TASK 5 — REPLACE HARD CORRELATION BLOCK WITH RELIABILITY-AWARE SIZE PENALTY
==================================================

Problem:
The hard correlation block is near-useless in practice and rarely or never fires. Sparse overlap also makes the estimate unreliable.

What to do:
- Do NOT over-engineer this.
- Keep the existing correlation computation if practical, but stop relying on it as a binary kill switch.
- Convert it into a sizing penalty.

Implementation guidance:
- Add reliability diagnostics such as:
  - aligned_days
  - overlap ratio
  - correlation available yes/no
- If correlation is unavailable or unreliable, treat it as unknown, not “safe”
- Apply a mild bounded size penalty when reliable correlation is high
- Keep behavior conservative and interpretable

Example behavior:
- reliable high corr => reduce size
- low reliability => neutral or mildly reduced size
- no more brittle hard blocking in normal flow unless a truly catastrophic threshold exists

Acceptance criteria:
- Correlation diagnostics appear in logs/reports
- Penalty application is visible in summaries
- Existing reporting remains usable

==================================================
TASK 6 — MAKE LIVE LOGS TRUTHFUL
==================================================

Problem:
The system logs “placing_order” even when no real exchange execution is wired.

What to do:
- Audit live_runner.py logs around order placement/opening.
- If there is no actual exchange adapter sending orders, do not log “placing_order”.
- Replace misleading logs with truthful labels such as:
  - [LIVE_SIGNAL_ONLY]
  - [PAPER_ENTRY]
  - [SIMULATED_OPEN]
- If execution mode claims live but no real adapter exists, emit a startup warning.

Acceptance criteria:
- No log message implies real order placement unless a real adapter path is actually invoked
- Startup warning appears when live mode lacks real execution wiring

==================================================
TASK 7 — ADD MINIMAL LIVE PRE-TRADE HALT SWITCHES
==================================================

Problem:
The live path lacks basic pre-trade halts for stale data and repeated API failures.

What to do:
- Add minimal, practical halt checks in live_runner.py before opening new positions.
- Add config options for at least:
  - halt_on_data_staleness
  - max_data_staleness_seconds
  - halt_on_api_errors
  - max_consecutive_api_errors
  - max_daily_loss_r (pre-trade block, not just sizing impact)

Behavior:
- When a halt condition is triggered, block NEW entries
- Keep managing / observing existing positions as appropriate
- Log exact halt reason clearly
- Make it obvious whether the system is halted or active

Acceptance criteria:
- New entries are blocked when halt conditions are met
- Exact halt reason is visible in logs/status
- Config is exposed and documented

==================================================
VALIDATION / DELIVERABLES
==================================================

After implementation:
1. List all changed files
2. Summarize each task completed
3. Show before/after behavior for:
   - size multiplier distribution
   - shared config loading
   - optional dependency startup behavior
   - symbol kill and recovery logging
   - correlation penalty diagnostics
   - live log truthfulness
   - halt switch behavior
4. Run relevant tests if present
5. Report any remaining known risks honestly

Do the work now. Do not just describe the plan.