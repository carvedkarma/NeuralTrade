You are in gpu_trainer + dashboard repo.

GOAL (PHASE 1):
Increase trade frequency to ~2–3 trades per asset per day WITHOUT collapsing PF_net by adding:
1) Per-asset rolling p_enter distribution tracking
2) Dynamic percentile-based thresholds
3) Dual policy engine: CORE + FLOW
4) Safety kill-switches and daily drawdown caps
Do NOT change ML model or features in this phase.

======================================================================
A) ADD PER-ASSET p_enter HISTORY + PERCENTILES
======================================================================

1) Maintain rolling p_enter history per symbol in live_runner (and/or portfolio manager):
- Dict[str, Deque[float]] p_enter_hist_by_symbol
- Keep last N=2000 values per symbol

2) Each cycle after inference:
- append p_enter for each symbol into its deque
- compute rolling percentiles per symbol:
  p50, p75, p90, p95, p97, p99
- log line per symbol:
  "SYMBOL p_enter percentiles (last N): p50=... p95=... p99=..."

3) Implement helper:
get_dynamic_threshold(symbol, mode):
- mode="CORE": threshold = max(abs_floor_core, percentile(symbol, core_pct))
- mode="FLOW": threshold = max(abs_floor_flow, percentile(symbol, flow_pct))
Defaults:
- core_pct = 99
- flow_pct = 95
- abs_floor_core = 0.35
- abs_floor_flow = 0.25
If history < 300 samples, fallback to static thresholds:
- core_static=0.85, flow_static=0.75

======================================================================
B) DUAL POLICY ENGINE: CORE + FLOW
======================================================================

4) Add a policy selection layer BEFORE entering trades:
- CORE policy: high selectivity, preserves edge
- FLOW policy: increases frequency but has stricter risk kill-switches

For each symbol each cycle:
- Determine CORE threshold = get_dynamic_threshold(symbol,"CORE")
- Determine FLOW threshold = get_dynamic_threshold(symbol,"FLOW")
- Candidate generation:
   - CORE candidate if (p_enter >= CORE_thr) AND HTF gates pass
   - FLOW candidate if (p_enter >= FLOW_thr) AND HTF gates pass AND regime_ok_for_flow

5) Regime gate for FLOW (simple, use existing HTF features — no new features):
- Allow FLOW only when:
  aligned == True AND slope_ok == True AND range_ok == True
- If not, FLOW is disabled for that symbol this cycle.

6) Geometry per policy (use existing trade geometry extraction logic):
- CORE geometry: tp_mult=3.0, sl_mult=1.25, cooldown=6, horizon=24
- FLOW geometry: tp_mult=2.5, sl_mult=1.5, cooldown=2, horizon=16
(Do not tune size-cap yet; keep size_cap=2.0)

7) Allocation & sizing:
- CORE uses normal sizing (existing)
- FLOW uses reduced risk multiplier by default: flow_risk_mult = 0.6
(So FLOW trades are smaller until proven)

======================================================================
C) SAFETY KILL-SWITCHES (NON-NEGOTIABLE)
======================================================================

8) Per-symbol daily drawdown cap:
- Track daily realized net_r per symbol.
- If daily_net_r(symbol) <= -2.0R => disable ALL trading for that symbol until next UTC day
Log: "KILL_SWITCH: SYMBOL disabled for day (DD cap hit)"

9) FLOW kill-switch based on rolling last K trades per symbol:
- Maintain last K=20 closed trades per symbol (net_r list)
- Compute rolling PF_net and rolling E_net
- If PF_net < 1.05 OR E_net < 0 => disable FLOW for that symbol until it recovers
Log: "FLOW_DISABLED: SYMBOL rolling PF/E failed"

10) Portfolio correlation cap remains in effect (do not loosen).
11) Always show reason when skipping:
- HTF_FAIL
- BELOW_CORE_THR (include value)
- BELOW_FLOW_THR (include value)
- FLOW_DISABLED
- DAILY_DD_CAP
- COOLDOWN

======================================================================
D) LOGGING + DASHBOARD OUTPUT (REQUIRED)
======================================================================

12) For every cycle per symbol log compact decision trace:
"SYMBOL p=0.42 CORE_thr=0.55 FLOW_thr=0.31 HTF(aligned=Y slope=Y range=Y) -> candidate=FLOW / HOLD reason=..."

13) Push to dashboard cycle log:
- p_enter
- CORE_thr, FLOW_thr
- selected policy (CORE/FLOW/NONE)
- hold reason

14) Add dashboard cards:
- CORE trades count, FLOW trades count (24h)
- CORE net R, FLOW net R (24h)
- FLOW enabled % time per symbol
- Average CORE_thr and FLOW_thr per symbol

======================================================================
E) VERIFICATION CHECKLIST (MUST PASS)
======================================================================

15) In logs we must see:
- "p_enter percentiles (last N) ..." per symbol
- "CORE_thr=..., FLOW_thr=..." per symbol
- FLOW trades created with "policy=FLOW"
- CORE trades created with "policy=CORE"
- FLOW_DISABLED logs when PF/E fails
- DAILY_DD_CAP logs when hit

16) No changes to model inference, feature set, or triple-barrier logic.

DELIVERABLE:
- Working live_runner that produces CORE/FLOW candidates per symbol
- System achieves higher trade frequency without removing HTF gates
