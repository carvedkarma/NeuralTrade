You are modifying a crypto trading repo. Make it safer, testable, and internally consistent tonight. Do NOT add new strategy complexity. Prioritize correctness, training/live parity, and backward compatibility.

GOALS:
1. Fix failing v5 logic tests.
2. Make optional dependencies non-fatal.
3. Align live and training config.
4. Convert fragile hard gates into safer behavior where possible.
5. Do not break existing CLI usage.

TASKS:

[1] FIX DIRECTIONAL SCORING IN train/v5_train.py
- Function: compute_v5_scores
- Keep side selection driven by action head probabilities in side_mode='action_head'
- After side selection, compute p_side from chosen side
- Use a clean breakeven-aware score formulation:
    mu_over_risk = abs(mu_R_adj) / max(risk, eps)
    raw_edge = mu_over_risk * ((1 + score_lambda) * p_side - score_lambda)
- This ensures score > 0 only when p_side > score_lambda / (1 + score_lambda)
- Preserve side_aware_scoring as a soft disagreement multiplier on edge, never hard-zero
- Preserve slippage adjustment and min_mu_r_score suppression
- Preserve min_p_side and min_p_short hard kills
- Return diagnostics with p_side_mean, breakeven, and counts of suppressed bars
- Add comments documenting the scoring equation

[2] FIX QUALITY GATE IN train/v5_train.py
- Function: v5_quality_mask
- When ref_arrays is provided, compute ALL adaptive thresholds from ref_arrays only
- Use arrays only for pass/fail masking, never for percentile estimation in that case
- Log whether thresholds came from TRAIN_REF or CURRENT arrays
- Keep warmup bypass behavior unchanged
- Keep relaxation logic, but start from thresholds estimated on ref_arrays
- Add diagnostics field threshold_source in return dict

[3] MAKE _run_v5_sweep BACKWARD-COMPATIBLE
- Function: _run_v5_sweep
- Add parameter return_full=False
- If return_full=False, return legacy 4-tuple:
    summary_dict, best_label, best_score_val, best_pct
  where summary_dict includes:
    total_trades, expectancy_r, pf, max_dd, trades_per_day, threshold
- If return_full=True, return the current expanded tuple or a structured dict
- Update internal callers to request return_full=True explicitly
- Do not break existing tests expecting 4 return values

[4] TIGHTEN _run_per_symbol_sweep
- Function: _run_per_symbol_sweep
- A symbol qualifies ACTIVE only if:
    expect > 0
    pf > 1.05
    total_r > 0
    trades >= min_trades_per_symbol
- Otherwise assign inf threshold (or global_threshold * 3 if existing convention required by callers)
- Ensure bad symbols are added to no_edge_symbols
- Add clearer logs for why a symbol was rejected

[5] MAKE OPTIONAL IMPORTS SAFE
- File: models/__init__.py
- Wrap sentiment imports in try/except ImportError
- If transformers unavailable, skip sentiment exports and log/debug note without crashing core imports

- File: data/pipeline.py
- Wrap pywt import in try/except ImportError
- Introduce HAVE_PYWT flag
- If pywt unavailable:
    - disable wavelet feature generation cleanly
    - return zero-filled or omitted wavelet features in a shape-stable way
    - do not crash FeatureEngineer
- Keep feature counts stable if tests expect fixed dimensions

[6] CREATE SHARED LIVE/TRAIN CONFIG
- Add new file: config/shared_v5_trade_config.py
- Define dataclass SharedV5TradeConfig with:
    score_threshold
    score_lambda
    min_mu_r_score
    min_p_side
    min_p_short
    slippage_base_bps
    corr_thresh
    cooldown
    side_mode
    side_aware_scoring
- Update train/v5_train.py, quick_start.py, and live_runner.py to read from this shared config
- Remove silent threshold mismatches between live and train defaults
- Set one sane default threshold based on current v5 scoring scale, not the old live hardcoded 0.5 unless explicitly overridden

[7] IMPROVE LIVE SAFETY IN live_runner.py
- Add explicit mode labels:
    paper
    signal_only
    live_disabled
- If actual exchange order placement is not implemented, do NOT log "placing_order"
- Change log to something truthful like:
    [LIVE_SIGNAL_ONLY] generated entry ...
- Add a startup warning if execution_mode='live' but no real exchange adapter exists

[8] PREP EXCHANGE EXECUTION INTERFACE
- Add file: execution/exchange_adapter.py or equivalent
- Define abstract adapter methods:
    place_entry_order(...)
    place_reduce_only_tp(...)
    place_stop_loss(...)
    cancel_order(...)
    fetch_open_positions(...)
    fetch_open_orders(...)
- Do NOT fully wire venue-specific logic tonight if too large
- But create the interface and route live_runner through it so fake-live and real-live are separated cleanly

[9] ADD A MINIMAL KILL SWITCH LAYER
- Add config flags:
    max_daily_loss_r
    max_total_open_risk_pct
    halt_on_data_staleness
    halt_on_api_errors
- In live_runner.py, block new entries when any kill switch is hit
- Log exact halt reason

[10] TESTING
- Run:
    pytest -q tests/test_v5_directional.py tests/test_v5_precision_audit.py tests/test_side_conditional.py tests/test_v5_features.py tests/test_correlation.py tests/test_drawdown_throttle.py tests/test_v5_position_sizer.py
- Fix until green or only clearly documented environment-skipped cases remain
- Print final summary of changed files and remaining known risks

DELIVERABLES:
- Code changes
- brief migration notes
- exact before/after test counts
- explicit list of what is still NOT safe for real-money trading