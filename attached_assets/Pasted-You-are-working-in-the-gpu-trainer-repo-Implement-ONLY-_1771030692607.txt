You are working in the gpu_trainer repo. Implement ONLY training/metrics tuning fixes (do NOT touch live trading engine thresholds/policies).

Goal: stop PR-AUC collapse after FULL stage switch by reducing “stacked aggressiveness” (focal + OHEM + edge_head + soft_labels).

Make these exact parameter changes + wiring:

1) OHEM strength
- Find where OHEM negative mining ratio/percent is configured (currently neg_pct=0.20).
- Change default to 0.08.
- If there is a CLI flag, set its default to 0.08 and ensure it is saved to train_config in checkpoints.
- If it is hardcoded, make it a config value and default to 0.08.

2) Edge head weight
- Find edge_head loss weight (currently 0.30).
- Change default to 0.15.
- Ensure it is saved in train_config in checkpoints.

3) Focal alpha
- Find focal loss alpha (currently 0.35).
- Change default alpha to 0.45. Keep gamma unchanged.
- Ensure it is saved in train_config in checkpoints.

4) Soft label temperature
- Find soft label temperature (currently temp=1.5).
- Change default temp to 1.2.
- Ensure it is saved in train_config in checkpoints.

5) Logging + verification
- On startup print a single summary block:
  [PR_TUNE] focal(gamma=..., alpha=...) ohem(neg_pct=...) edge_head(weight=...) soft_labels(temp=...)
- During eval, keep existing [PENTER_AUDIT], [SEP_CHECK], PR-AUC, Pos%, Pred% logs.
- Add a guard: if PR-AUC drops by >0.06 immediately after switching from WARMUP->FULL, log:
  [ALERT] PR-AUC collapse after FULL switch — check OHEM/edge/soft/focal config.

6) Run test training
- Run training for 20 epochs on the same multi-asset setup (BTC/ETH/SOL).
- Paste logs for epochs 8-20 showing: stage, PR-AUC, sep, Pos%, Pred%, p_enter percentiles.

Acceptance criteria:
- No major PR-AUC collapse after epoch 11 FULL-stage switch.
- Pred% remains non-zero and roughly in 8–25% range.
- sep remains stable (no sudden drop to near 0).
- p99 > p75 > p50 monotonic.

Deliverables:
- code changes committed
- updated defaults + CLI/config wiring + train_config checkpoint saving verified
- pasted logs (epochs 8-20)
