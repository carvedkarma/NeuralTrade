TASK: Fix why the multi-head trainer produces ZERO trades (all HOLD) even when config says 15m horizon=10 and costs=0.09%.

Symptoms from logs:
- Class distribution stays identical: SHORT=47, NEUTRAL=24284, LONG=81
- Validation has “No directional trades” every epoch
This proves label generation is still using old params (horizon/cost/thresholds) or gating is too strict.

GOAL:
1) Ensure the EXACT config (timeframe=15m, horizon=10 bars, taker_taker=0.09%) is used inside target generation.
2) Add instrumentation to print label stats + gating stats BEFORE training.
3) Adjust thresholds so we get a sane label density (not 99% HOLD).

IMPLEMENT THESE STEPS:

A) Trace config plumbing (must be deterministic)
- Find where horizon is defined for training and where targets are generated.
- Ensure the SAME horizon value is passed into target generator.
- Ensure the SAME cost settings are used inside the target generator (no hidden volatility slippage unless explicitly enabled).
- Add explicit logging at target generation time:
  - timeframe, horizon, cost_mode, avg_cost, min_net_edge, min_confidence

B) Add “label density debug report”
Right after generate_multihead_targets(), compute and log:
- total samples
- counts and percentages for {SHORT,HOLD,LONG}
- mean(|mu|), mean(sigma), mean(|mu|/sigma)
- mean(cost), mean(|mu|-cost) = net_edge
- % passing net_edge gate
- % passing confidence gate
- % passing both gates
If % passing both < 1%, print WARNING and dump the gate thresholds.

C) Fix the root issue: horizon/cost mismatch
- If the target generator has defaults or hardcoded horizon/cost, remove them.
- Make target generator signature require (horizon, costs_config, thresholds_config) and only use those.
- Ensure trainer calls it with the config loaded from CLI/config file.

D) Make validation not “zero-trade”
- Use stratified or “trade-aware” time split so validation contains at least some directional labels.
- Or add a fallback: if val has zero trades, compute metrics on train only + skip “no trades” warning spam.

E) Temporary safe thresholds to restore trades (for debugging)
- Set min_net_edge = 0.0
- Set min_confidence = 0.3
- Re-generate labels and confirm target density: aim HOLD 70–85%, LONG 7–15%, SHORT 7–15%.
- After trades exist, tune thresholds upward gradually.

F) IMPORTANT: do NOT change model architecture yet
We only fix label generation + gating + validation split so trades exist.
Once label density is healthy, we evaluate backtest expectancy and then tune model.

DELIVERABLES:
1) A PR/commit that fixes config wiring into label generation (horizon/cost/thresholds).
2) The debug report logs printed once per run.
3) Updated training logs showing class distribution changed (NOT 47/24284/81) and validation contains directional trades.
4) A small unit test that asserts label distribution changes when horizon/cost changes (sanity check).

Run commands and paste logs:
- Run with horizon=10 cost=0.09% and show label stats.
- Run with horizon=5 cost=0.09% and show label stats differ.
- Run with horizon=10 cost=0.50% and show label stats differ.