We need to fix pattern learning. Right now we have 10,500 candles, 480 trades, and 1,220 patterns → this is extreme over-fragmentation (0.39 trades/pattern). Implement the following immediately:

1) Hard cap patterns + minimum support

Set:

MAX_PATTERNS_TOTAL = 30 (temporary)

MIN_SAMPLES_PER_PATTERN = 50 (temporary; later raise to 100)

Any pattern with samples < MIN must be:

merged into nearest larger pattern OR

discarded / not used for signals

Do not create new patterns if the cap is reached.

2) Regime-specific patterns only

Patterns must exist inside these regimes only:

trend_up, trend_down, chop, shock
Never mix regimes in the same pattern cluster.

3) Freeze new pattern creation until data grows

Disable new pattern discovery until:

backtest_trades >= 1000 AND candles_15m >= 30000
Until then, only update stats of existing patterns.

4) Fix similarity system (avg similarity must not be 100%)

Add these safeguards:

Exclude the current window from nearest-neighbor search

Apply an embargo of H candles (horizon) so we never match near-future overlaps

Log similarity distribution per query: min/mean/max
Target: avg similarity should be ~65–85%, not 100%

If embeddings are zero/identical, fix normalization.

5) Reduce pattern influence on decisions (for now)

Pattern memory should be a veto/confidence modifier, not the main driver.
Set weight low (example):

w_pattern = 0.15, w_ml = 0.55, w_rules = 0.30
And only allow pattern vote if pattern maturity is high.

6) Add “pattern maturity score”

For each pattern cluster, compute:

support = count

stability = win_rate_confidence_interval_width (or std)

maturity = clamp(support / 200, 0..1) (simple MVP)
Only allow patterns into decision logic when:

support >= 50 AND maturity >= 0.25

7) UI changes (stop misleading outputs)

Replace “patterns learned 1220” with:

“active patterns: X (cap 30)”

“immature patterns: Y”

Hide pattern win rates unless support >= MIN_SAMPLES_PER_PATTERN

If sample size low, show “insufficient data”

8) Recompute everything after changes

After implementing, rerun:

pattern clustering

pattern stats

backtest metrics
And report:

total patterns active

patterns per regime

avg similarity min/mean/max

trades counted (excluding HOLD/chop)

Expected result

Patterns drop from 1220 → <=30

Similarity is no longer stuck at 100%

HOLD frequency remains high (good)

Win rate stabilizes (don’t optimize yet)