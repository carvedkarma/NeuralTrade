You are my Replit Agent. Task: extend the v5 system from 3 assets (BTCUSDT, ETHUSDT, SOLUSDT) to include 4 more assets: BNBUSDT, AVAXUSDT, XRPUSDT, ADAUSDT — end-to-end (data → training → walk-forward → forward-test → live/paper). Do NOT change model architecture unless required; focus on plumbing + robustness.

GOALS
- New symbols work everywhere the old 3 did.
- Walk-forward + forward-test runs without missing-data failures.
- Symbol sampling is balanced so BTC doesn’t dominate training.
- Correlation blocker + caps still work with 7 symbols.

STEP 1 — SYMBOL LIST / CONFIG
1) Find the default symbols list and any hard-coded asset lists.
   - Update defaults to: BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT,AVAXUSDT,XRPUSDT,ADAUSDT
2) Ensure quick_start.py accepts these symbols via --symbols and passes them through to:
   - data download/loader
   - training
   - walk-forward
   - forward test
   - live/paper

STEP 2 — DATA DOWNLOADING / CACHING
1) Update downloader to fetch OHLCV for the new 4 symbols for the chosen interval(s) (15m).
2) Add “download missing data” support for these symbols automatically when requested:
   - Respect --download-missing-data and --allow-partial-data behavior
3) Add clear logging:
   - For each symbol: start/end date, bars downloaded, bars available, any gaps.

STEP 3 — DATASET / DATALOADER BALANCING (IMPORTANT)
To avoid BTC dominating:
1) Implement symbol-balanced sampling:
   - Each batch should include roughly equal samples per symbol, or
   - Cap max samples per symbol per epoch to the smallest symbol’s available bars.
2) Add a log each epoch:
   - samples_per_symbol used in that epoch
3) If current pipeline uses a single scaler across all symbols, KEEP it for now unless it breaks;
   but add a TODO and a switch:
   - --per-symbol-scaler (OFF by default)
   - When ON: fit/apply scaler per symbol and save/load per-symbol scalers.

STEP 4 — CORRELATION BLOCKER COMPATIBILITY
1) Confirm corr matrix includes all 7 symbols.
2) Ensure corr blocker uses the expanded symbol set and does not crash on missing pairs.
3) Keep existing CLI flags working:
   - --v5-corr-block --v5-corr-window-days --v5-corr-thresh --v5-corr-same-side-only
4) Add summary logging:
   - mean_abs_corr, max_abs_corr, overlap_ratio, blocked_trades
   - top correlated pair

STEP 5 — TRAIN / WALK-FORWARD / FORWARD-TEST
1) Run:
   - --v5-walk-forward (12/1) + --v5-forward-test + --v5-freeze-decision
   using the 7 symbols.
2) Ensure checkpoints, reports, and best-policy outputs are saved and include symbol info.
3) Ensure any per-symbol limits are applied correctly:
   - --max-pos-symbol
   - --risk-cap-symbol
   - --v5-per-symbol-daily-r

STEP 6 — LIVE/PAPER MODE
1) Ensure the “universe” contains the 7 symbols and that:
   - signal generation
   - gating (EMA200 + ADX if enabled)
   - correlation blocking
   - execution router (paper)
   works per-symbol without crashes.
2) Confirm record-trades writes symbol field and aggregation can handle 7 assets.

STEP 7 — TESTS + SAFETY
1) Add unit tests / integration smoke tests:
   - dataset loads all 7 symbols
   - downloader returns non-empty for each
   - corr matrix generation includes all 7
   - walk-forward runs at least 1 fold successfully for all 7 (smoke)
2) If any symbol has insufficient history:
   - Either fail with a clear message OR honor --allow-partial-data and proceed.

DELIVERABLES
1) PR-level changes committed.
2) Update replit.md with:
   - one “train + walk-forward” command using 7 symbols
   - one “paper live” command using 7 symbols
3) Paste a final summary showing:
   - bars per symbol
   - trades/day per symbol (paper/forward test)
   - corr stats
   - any blocked trades counts

REFERENCE COMMANDS TO VALIDATE
A) Walk-forward + forward test:
python quick_start.py --train-v5 --v5-staged-training --v5-promote-metric expectancy --v5-temp-scale --v5-walk-forward --v5-wf-train-months 12 --v5-wf-test-months 1 --v5-forward-test --v5-freeze-decision --epochs 80 --symbols BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT,AVAXUSDT,XRPUSDT,ADAUSDT --interval 15m --tp-mult 2.3 --sl-mult 1.5 --horizon 20 --cooldown 3 --use-regime-head --v5-ema200-regime-gate --v5-adx-gate --v5-adx-min 16 --v5-min-threshold 0.08 --v5-target-tpd 1.8 --v5-tpd-tol 0.8 --v5-max-trades-per-day 3 --v5-weekly-loss-cap -6.0 --v5-daily-loss-cap -5.0 --v5-per-symbol-daily-r -3.0 --v5-corr-block --v5-corr-window-days 30 --v5-corr-thresh 0.85 --v5-corr-same-side-only --v5-adaptive-sizing --v5-kelly-fraction 0.35 --v5-min-size-mult 0.8 --v5-max-size-mult 2.3 --v5-regime-scaling --v5-regime-bull-mult 1.4 --v5-regime-bear-mult 0.8 --v5-warmup-skip-bars 96

B) Paper live:
python quick_start.py --live --paper --execution-mode paper --symbols BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT,AVAXUSDT,XRPUSDT,ADAUSDT --interval 15m --tp-mult 2.3 --sl-mult 1.5 --horizon 20 --cooldown 3 --v5-freeze-decision --v5-ema200-regime-gate --v5-adx-gate --v5-adx-min 16 --v5-min-threshold 0.08 --v5-corr-block --v5-corr-window-days 30 --v5-corr-thresh 0.85 --v5-corr-same-side-only --v5-weekly-loss-cap -6.0 --v5-daily-loss-cap -5.0 --v5-per-symbol-daily-r -3.0 --record-trades