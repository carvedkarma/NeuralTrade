You are working in gpu_trainer only. Implement the requested training/metrics-only fixes. Do NOT change live trading engine thresholds, policies, or execution logic.

Goal: Fix ENTER classifier early collapse (Pred%=0) and ensure p_enter percentiles come from the exact same tensor used for PR-AUC.

========================
1) Two-stage loss schedule (warmup -> full)
========================

Where: find the ENTER classifier training function (likely train_enter_model / enter_model trainer) where enter_logits and enter_labels are computed and loss is built.

Add CLI flags to argparse:
  --loss-warmup-epochs (int, default=10)
  --warmup-pos-weight (float, default=2.0)

Wire into config object used by training (LearningConfig / TrainConfig).
Ensure both keys are saved into train_config inside checkpoints, same as other flags.

Implement stage logic by epoch index:

Warmup stage (epoch < loss_warmup_epochs):
  - Use plain BCEWithLogitsLoss ONLY (no focal)
  - Disable OHEM (use all samples in batch)
  - Use a mild pos_weight, capped:
      warm_pos_w = min(configured_pos_weight, warmup_pos_weight)
    If you compute pos_weight from data, cap it to warmup_pos_weight during warmup.
  - Log once per epoch:
      [LOSS_STAGE] stage=WARMUP epoch=E pos_weight=...

Post-warmup stage (epoch >= loss_warmup_epochs):
  - Enable focal loss if configured (gamma/alpha)
  - Enable OHEM if configured (all positives + top X% hardest negatives)
  - Restore configured pos_weight (no warm cap)
  - Log once per epoch:
      [LOSS_STAGE] stage=FULL epoch=E pos_weight=... focal=on/off ohem=on/off

Important: DO NOT change the trading thresholds/policies. Only training pipeline.

========================
2) Fix p_enter percentiles to use SAME tensor as PR-AUC
========================

During validation/eval:
  - Ensure PR-AUC is computed from:
      p = sigmoid(enter_logits)
    where enter_logits are the raw logits from the model for the current val set.
  - Percentiles must be computed from that same p tensor (no calibration, no policy mapping, no thresholding).
  - Add once-per-eval audit line:
      [PENTER_AUDIT] p_min=... p_max=... p_mean=... logits_min=... logits_max=...
  - If any NaN/Inf in logits or p:
      raise RuntimeError with a clear message including which tensor had NaNs/Infs.

Also ensure the printed "p_enter percentiles (val)" lines are derived from this same p tensor.
Add a monotonic sanity line (optional but helpful):
  [PENTER_PCTL] p50=... p75=... p90=... p95=... p99=...

========================
3) Verification mode: --verify-enter-metrics
========================

Add CLI flag:
  --verify-enter-metrics

When enabled:
  - Run 3 validation passes (same dataloader) without training updates.
  - For each pass, compute:
      Pred% (fraction p>=0.5)  (or use the same Pred% definition you currently log)
      sep = mean_logit_pos - mean_logit_neg (on that pass)
      percentiles p75 and p99
  - Assertions:
      A) During warmup (simulate by calling eval after epoch 1 using warmup stage settings),
         Pred% must be between 2% and 60% (not 0%).
      B) sep must increase from epoch 1 to epoch 10 (train for 10 epochs then compare).
      C) p99 > p75 (monotonic check)

Write report file verify_enter_metrics.md containing:
  - pass results table (Pred%, PR-AUC, sep, p75, p99)
  - assertion outcomes (PASS/FAIL)
  - if fail, include last [PENTER_AUDIT] line for debugging.

========================
4) After implementing: run a 15-epoch training and paste key logs
========================

Run training for 15 epochs and paste logs showing:
  - [LOSS_STAGE] each epoch
  - Pred%, PR-AUC, sep each epoch
  - p_enter percentiles each eval
  - [PENTER_AUDIT] each eval

Do not modify any live trading components.
Commit changes cleanly and keep formatting consistent with existing log style.
