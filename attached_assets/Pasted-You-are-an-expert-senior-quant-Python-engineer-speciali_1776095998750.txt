You are an expert senior quant Python engineer specializing in high-frequency crypto trading systems.

I have a complete V5 neural network BTC futures trading system in the gpu_trainer/ folder. It features:
- Two specialist models (dedicated LONG and SHORT heads)
- Multi-head outputs (direction + quantiles + μ + σ + vol_state + acceleration)
- EnsemblePredictor that weights models by real walk-forward trading metrics (expectancy, Sharpe, profit factor, etc.)
- Unified trade gating in utils/trade_gating.py (edge + confidence + spread + margin)
- Walk-forward evaluation with per-symbol thresholds
- FastAPI prediction server with drift monitoring
- Comprehensive validation harness (validate.py)

I just received a detailed code audit. The system is already very strong (production-grade), but it needs the following targeted improvements:

High priority:
1. Enforce a single canonical model_meta.json (saved during training) that contains model type, version, feature schema, and is_multihead flag. Update model loading in api/server.py to read this first and fail fast if missing or mismatched. Remove the fragile 70+ pattern matching.
2. Standardize model_weights.json path to checkpoints/model_weights.json only (remove multiple candidate paths). Keep strict_weights=True in production.
3. Add the spread gate check (q75 - q25 >= multiplier * fixed_cost) directly into compute_trade_gate() in utils/trade_gating.py so it is enforced consistently in both training evaluation and live inference.

Medium priority:
4. Add a hard daily R-budget guard before executing any trade (if lane_budget_remaining_r < -0.01, block with reason "BUDGET_EXCEEDED").
5. Implement a fast_predict mode in EnsemblePredictor that skips MC Dropout and regime models (VAE/GNN) when confidence is already high, for lower latency in live polling.
6. Make sure TradeGateConfig defaults are imported from shared_v5_trade_config.py instead of being duplicated.

Low priority / polish:
7. Update README.md and GPU_SETUP_GUIDE.md to reflect current V5 architecture (remove outdated mentions of old models like RL agent, sentiment, etc.).
8. Add proper versioning and a live_sim validation mode if possible.

Task for you:
- First, read and understand the entire gpu_trainer/ codebase (especially api/server.py, api/ensemble_predictor.py, utils/trade_gating.py, validate.py, and training-related files).
- Propose a clear, step-by-step implementation plan before making any code changes.
- Make the changes with minimal disruption to existing functionality.
- After changes, run `python validate.py unit_audit` and `python validate.py smoke_wf` to verify nothing is broken.
- Finally, output a short summary of what was changed and any new files created.

Do not delete or refactor large unrelated sections. Keep changes surgical and well-commented. Ask me for clarification if anything is ambiguous.