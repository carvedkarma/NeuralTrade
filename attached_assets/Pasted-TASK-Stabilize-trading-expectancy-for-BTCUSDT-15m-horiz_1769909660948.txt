TASK: Stabilize trading expectancy for BTCUSDT 15m (horizon=16) by improving the EXECUTION POLICY (trade filter). We already see some epochs with positive expectancy (e.g. Expectancy +0.0013, HitRate ~59%, Trades ~114) but most epochs are negative because we overtrade and include marginal signals.

DO NOT change model architecture or features. Only change trade decision / evaluation logic.

IMPLEMENT 4 CHANGES:

1) Add a SECOND hard gate based on distribution width (quantile spread)
- We already have quantile outputs (or can compute q25/q75).
- Define:
    spread = q75 - q25   (in return units)
    cost = fixed_cost (0.0009) or computed per trade
- Gate rule:
    require spread >= K * cost
  Start with K=3 (also test K=4,5)
This removes chop trades where the model “has direction” but not enough room to move.

2) Raise min_confidence from 0.3 to a sweep + choose best
- Confidence = |mu| / sigma
- Add an eval sweep without retraining:
    min_confidence in [0.3, 0.5, 0.7, 0.9, 1.1]
- For each threshold, compute:
    Trades, Expectancy, HitRate, AvgWin, AvgLoss, MaxDD, Sharpe
Pick the threshold that gives positive expectancy with reasonable trades.
Expected sweet spot for BTC 15m h=16 is ~0.6–0.8.

3) Add a cooldown to prevent signal spam
- After taking a trade, do not open another for COOLDOWN candles.
- Set COOLDOWN = horizon/2 (8 candles) initially.
This stops repeated entries in the same noise regime and stabilizes results.

4) Make SL/TP asymmetric using quantiles (fix “50% hit rate but still losing”)
- If we already compute quantiles:
    SL distance from q10 (or q15)
    TP from q75 or q90
- Example:
    tp_ret = q75 (or q90 for strong signals)
    sl_ret = q10
- This ensures AvgWin >= AvgLoss and expectancy can be positive even with <50% win rate.

DELIVERABLES:
A) Implement the gates in the evaluation/trading_metrics path (not training).
B) Print a “Policy Report” per epoch:
   - min_confidence, K, cooldown
   - Trades, Expectancy, HitRate, AvgWin, AvgLoss, Sharpe
C) Run sweep and report the best setting.
D) Confirm that with best settings expectancy stays positive across multiple epochs/folds, not just one.

NOTES:
- Keep STF 15m only.
- Keep horizon=16.
- Keep fixed_cost=0.0009.
- If quantiles are not available in evaluation, compute them from the model outputs or add q25/q75 heads (pinball loss) BUT do not change feature set.
