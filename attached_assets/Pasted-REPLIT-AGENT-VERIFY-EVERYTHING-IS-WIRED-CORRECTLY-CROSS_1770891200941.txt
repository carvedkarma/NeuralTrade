REPLIT AGENT — “VERIFY EVERYTHING IS WIRED CORRECTLY” (CROSS + FLOW + Lanes + Quota + Dashboard)

You are working in the repo (dashboard + server + ingest + GPU trainer). Your job is NOT to add new features.
Your job is to AUDIT and PROVE that the existing v4.3.x system is applied correctly end-to-end.

GOAL:
Produce a single verification report (printed in console + saved to verify_report.md) that answers:
✅ Are CROSS rules applied correctly?
✅ Does FLOW actually trigger with lowered thresholds when behind quota?
✅ Are lane router priorities correct (CORE > FLOW > SCALP > HOLD)?
✅ Are quota fields arriving in DB + API + UI?
✅ Are trade lifecycle + net_r accounting correct?

DO THIS IN 4 PHASES:

========================================================
PHASE 1 — Static Code Audit (router + cross + quota wiring)
========================================================
1) Locate lane router code in GPU trainer (live_runner.py or equivalent).
Confirm:
- It contains explicit ordered routing:
  if CORE pass -> CORE
  elif FLOW pass -> FLOW
  elif SCALP pass -> SCALP
  else HOLD
- It is NOT using a single global threshold (0.85) for all lanes.

2) Locate correlation blocking / CROSS logic (portfolio.py).
Confirm:
- It blocks only SAME-DIRECTION correlated exposure (e.g., BTC+ETH both LONG).
- It does NOT block opposite directions.
- It checks exposure caps BEFORE placing the second position.
- It logs a specific reason when blocking.

3) Locate quota controller logic.
Confirm:
- It outputs fields: quota_step, trades_today, target_tpd, quota_pressure.
- It updates each cycle and resets at UTC midnight.
- It feeds FLOW thresholds (percentile stepping).

Output in report:
- File paths + function names (no vague statements).
- A short bullet list of what was found.

========================================================
PHASE 2 — Instrumentation: Add “must-have logs” (only if missing)
========================================================
Add or ensure these log lines exist (exact prefixes required):

[LANE_DECISION] sym=... lane=CORE|FLOW|SCALP|HOLD p=... htf_score=... core_thr=... flow_thr=... scalp_thr=... reason=...
[CROSS_BLOCK] sym=... blocked_with=... dir=... reason=...
[QUOTA] sym=... trades_today=... target_tpd=... step=... pressure=...
[CYCLE_PAYLOAD] sym=... fields_present=[...keys...]

Also ensure cost accounting invariant exists in debug mode:
[NET_CHECK] net_r == gross_r - cost_r (tolerance 1e-6)

========================================================
PHASE 3 — End-to-End Runtime Verification (paper mode)
========================================================
Add a CLI flag:
--verify-system
When enabled, run for N cycles (default 30) and enforce assertions:

A) Lane routing assertions:
- If CORE conditions pass, lane must be CORE (never FLOW/SCALP).
- FLOW may only occur if CORE failed.
- SCALP may only occur if CORE+FLOW failed.
- HOLD must include hold_reason.

B) FLOW threshold assertions:
- When quota_pressure >= 2, flow_thr must be < core_thr at least once within 30 cycles.
- If flow_thr never differs from core_thr, FAIL verification.

C) CROSS assertions:
- If a position exists in BTC LONG and ETH LONG candidate arrives, CROSS may block.
- If BTC LONG and ETH SHORT candidate arrives, MUST NOT block due to correlation.
- Log [CROSS_BLOCK] only for same-direction blocks.

D) Payload assertions:
- Every cycle sent to ingest must include:
  lane_selected, hold_reason,
  quota_step, trades_today, target_tpd, quota_pressure,
  core_thr, flow_thr, scalp_thr (can be null if lane not evaluated but must exist as keys).
If missing, FAIL.

At end, print:
VERIFICATION PASSED or VERIFICATION FAILED
with the first 10 failures listed.

========================================================
PHASE 4 — DB + API + UI Verification
========================================================
1) Server: Add a debug endpoint (dev-only):
GET /api/debug/latest-cycle?symbol=BTCUSDT
Returns the last cycle row including quota fields + thresholds.

2) Verify DB actually stores quota fields:
- Query last 50 cycle_logs rows; compute % rows with quota_step not null.
- If < 90%, FAIL.

3) Verify UI:
- If summary contains quota data, Quota Status card must not show “No quota data yet”.
- FLOW Engine Status must show avg flow_thr once at least 1 FLOW cycle exists.
- Hold Reasons should include threshold strings for lane holds (not only “p_enter < 0.85”).

Write these checks to verify_report.md.

========================================================
DELIVERABLES
========================================================
1) verify_report.md saved to repo root containing:
- Summary PASS/FAIL table
- Evidence: sample log lines (3 examples per category)
- If FAIL: exact missing fields + file where fix is needed

2) A single command to run verification:
python quick_start.py --paper --verify-system --cycles 30

DO NOT implement MICRO or any new trading logic until verification passes.
