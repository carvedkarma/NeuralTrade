You are my Replit Agent. Implement OPTION 1 (multi-asset) + OPTION 2 (lower-TF execution) for my BTC “ENTER Quality” trading system, WITHOUT changing training/labels/model. The goal is to safely increase frequency to ~2–3 trades/day without collapsing PF.

========================
NON-NEGOTIABLE RULES
========================
1) DO NOT change training, labeling, model architecture, feature pipeline versions, or triple_barrier logic.
   - Keep current model + enter_head inference exactly the same.
2) DO NOT loosen thresholds just to force trades.
3) All frequency increase must come from:
   A) Multi-asset parallelization (Option 1)
   B) Better execution entries on lower TF after a 15m ENTER signal (Option 2)
4) Keep ALL existing HTF gates (1H/4H trend alignment, slope strength, range position checks).
5) Risk controls must prevent correlated blowups (portfolio caps, per-asset caps).

========================
CURRENT SYSTEM CONTEXT
========================
- Model: ENTER quality binary classifier (enter_head), using 63 features (47 STF + 10 HTF + 3 funding + 3 OI)
- Horizon: 24 bars on 15m (~6 hours)
- Policy currently validated:
  - threshold ≈ 0.85
  - TP/SL geometry uses ATR-based multipliers (example proven: TP=3.5x ATR, SL=1.5x ATR, cooldown=8)
- Costs realism exists (fees/spread/slippage) and regime eval exists.
- There is a CLI entrypoint: python quick_start.py --predict (and training / regime-eval / geometry-sweep exist)

========================
TASK A — MULTI-ASSET LIVE INFERENCE LOOP (OPTION 1)
========================
Implement an always-on “live runner” mode that:
- Monitors multiple symbols in parallel: default ["BTCUSDT","ETHUSDT","SOLUSDT"] (configurable)
- Every 15 minutes:
  1) Fetch latest candles per symbol
  2) Compute features exactly as current pipeline does
  3) Run make_enter_prediction (single-row inference) using the loaded checkpoint
  4) Apply HTF gates (unchanged)
  5) Produce a candidate trade if ENTER signal passes p_enter threshold
- Rank candidates and only allow limited entries:
  - max_open_positions_total = 2 (configurable)
  - max_open_positions_per_symbol = 1
- If multiple candidates appear same cycle, choose by:
  1) Highest p_enter
  2) If tie: higher ATR-normalized expected net R (if available)
  3) If still tie: lower correlation proxy (prefer ETH/SOL over BTC+ETH simultaneously) — keep simple rule-based.

Implementation details:
- Add CLI: 
  python quick_start.py --live --symbols BTCUSDT,ETHUSDT,SOLUSDT --interval 15m
- Must not exit after 1 prediction. It runs continuously.
- It should log decisions and also push to the existing dashboard endpoint per symbol (include symbol in payload).
- Add persistent state:
  - track open positions per symbol (paper mode)
  - enforce cooldown per symbol based on 15m bars
  - do not re-enter if position already open

Paper/live mode:
- Add flag --paper (default true). If true, no exchange orders, only logs and dashboard pushes.
- If later we wire real orders, keep interface clean.

========================
TASK B — LOWER TIMEFRAME EXECUTION (OPTION 2, SAFE VERSION)
========================
After a 15m signal qualifies for entry, do NOT market-enter immediately.
Instead implement an “execution module” that tries to improve fill using 1m or 3m microstructure, WITHOUT changing TP/SL geometry.

Rules:
- Signal timeframe remains 15m (unchanged).
- Execution timeframe: configurable --exec-tf 1m or 3m (default 3m for stability).
- Execution window: max 15 minutes after signal.
- For LONG:
  - wait for a pullback of pullback_atr = 0.15 to 0.30 ATR(14) (configurable)
  - confirmation: price reclaims VWAP (or EMA20) on exec TF + a green close
  - then enter
- For SHORT:
  - symmetric rules (pullback up then reject below VWAP/EMA20)

If pullback/confirmation never occurs within window:
- fall back to entering at market (optional, default OFF to avoid bad fills)
- instead, mark “missed entry” and skip trade (default behavior)

Add CLI knobs:
- --exec-tf {1m,3m}
- --exec-window-min 15
- --pullback-atr 0.20
- --confirm-indicator {vwap,ema20} default vwap
- --allow-market-fallback false (default)

Logging must show:
- signal time, p_enter, HTF direction
- chosen exec TF
- pullback hit or not
- confirmation hit or not
- entry price used vs original signal close
- expected cost impact reduction estimate (optional)

========================
TASK C — PORTFOLIO RISK CONTROLS (ANTI-COLLAPSE)
========================
Implement basic risk controls:
- Position sizing remains as existing confidence sizing (do not change math), but add portfolio caps:
  - cap total risk: sum(position_risk_pct) <= 10% (configurable)
  - cap per symbol risk <= 5%
- Deny new trade if:
  - already at max positions
  - symbol position already open
  - portfolio risk cap exceeded
  - same-direction correlation rule: if BTC and ETH both signal same direction, only take the higher p_enter (configurable toggle)

========================
TASK D — CODE STRUCTURE
========================
Create new modules (or similar):
- live_runner.py   (scheduling loop, multi-symbol orchestration)
- execution.py     (lower TF entry logic)
- portfolio.py     (position tracking, caps, cooldowns)
Do not break existing quick_start commands.
Integrate via new CLI flags in quick_start.py:
- --live
- --symbols
- --exec-tf
- --paper
- --max-pos-total
- --max-pos-symbol
- --risk-cap-total
- --risk-cap-symbol
- --allow-market-fallback

========================
TASK E — TESTS / VALIDATION (MUST)
========================
1) Unit test: ensure live loop calls the exact same make_enter_prediction path as --predict
2) Unit test: execution rules are symmetric long/short and never use future candles.
3) Smoke test: run --live in paper mode for 1 hour and verify:
   - no crashes
   - pushes per symbol include symbol
   - cooldown works
4) Add a “dry-run” mode:
   --dry-run : doesn’t fetch from Binance; replays last N candles from cache for each symbol.

========================
DELIVERABLES
========================
- Updated code with the new live multi-asset + execution modules
- Updated CLI help text and README snippet:
  Example run:
  python quick_start.py --live --paper --symbols BTCUSDT,ETHUSDT,SOLUSDT --interval 15m --exec-tf 3m --pullback-atr 0.20
- Clear logging so we can diagnose why a trade was skipped.

Start implementing now. Do not ask questions unless something is genuinely ambiguous; make reasonable defaults as stated above.
