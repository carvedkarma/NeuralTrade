You are working inside the GPU trainer codebase. Do a focused audit and fix pass for **low specialist thresholds / weak specialist score separation** in the V5 pipeline.

## Goal

We are getting very low thresholds on both LONG and SHORT specialists and poor trading results. I need you to determine whether the issue is:

1. weak model score separation,
2. threshold sweep / reference calibration bug,
3. overly hard gating,
4. specialist scoring design,
   or a combination.

Do **not** give generic comments. Make precise code changes, add diagnostics, run targeted tests, and report exactly what changed.

---

## Files to inspect first

* `train/v5_train.py`
* `models/v5_forecaster.py`
* `training/multihead_loss.py`
* `tests/test_v5_directional.py`
* `tests/test_v5_precision_audit.py`

---

## Known hotspots already identified

### 1) Specialist score construction

Inspect and instrument:

* `compute_v5_scores()` around lines ~1522–1705 in `train/v5_train.py`

Current concerns:

* specialist scores are built mainly from `effective_p * mu_over_risk - penalty`
* this may collapse score magnitude near zero when `mu_R` is weak
* `score_pside_weight` may be making specialist scores too dependent on weak action confidence
* soft disagree multipliers may still leave no useful right tail

### 2) Forward/reference score construction disables `min_mu_r_score`

Inspect these call sites:

* around line ~2701 in `train/v5_train.py`
* around line ~3152 in `train/v5_train.py`

Current issue:

* both training reference score build and forward-test score build pass `min_mu_r_score=0.0`
* this likely allows too many tiny-edge bars into the reference distribution and threshold sweep
* I want this reviewed and fixed properly

### 3) Hard-kill gating in score computation

Inspect in `compute_v5_scores()`:

* `min_p_side`
* `min_p_short`
* specialist mu_R gates
* any `scores[mask] = -inf` behavior

Current concern:

* good tail may be getting killed before threshold sweep
* low thresholds may simply reflect a damaged candidate pool

### 4) Per-symbol / per-side threshold blocking

Inspect forward threshold logic around:

* lines ~3331–3425 in `train/v5_train.py`

Current concern:

* `inf` thresholds / HIGH_BAR states may be turning thresholding into a no-trade artifact
* need stronger diagnostics and safer fallback behavior

### 5) Head disagreement hard block

Inspect around:

* lines ~4087–4108 in `train/v5_train.py`

Current concern:

* this may be deleting too many specialist candidates after score ranking
* need diagnostics to quantify actual damage

---

## Required tasks

### Task A — Add specialist diagnostics

Add detailed diagnostics for BOTH LONG and SHORT specialists.

For each specialist, log:

* total bars
* finite-score bars
* mean / std of score
* p50 / p75 / p90 / p95 / p99 of score
* mean / p90 of `|mu_R|`
* mean `mu_over_risk`
* percentage where return-head sign agrees with specialist direction
* percentage killed by:

  * `min_p_side`
  * `min_p_short`
  * `min_mu_r_score`
  * `min_mu_r_long`
  * `max_mu_r_short`
  * any other hard `-inf` path
* threshold candidates tried in sweep
* selected threshold
* trade count and expectancy for each threshold candidate

I want logs that clearly answer:
“Are thresholds low because the score cloud is weak, or because gates killed the good tail?”

### Task B — Fix reference and forward score-floor handling

Review whether `min_mu_r_score=0.0` is appropriate at:

* training reference build
* forward test scoring
* any specialist threshold calibration path

Implement a safer approach:

* do **not** let negligible-edge bars dominate the score reference distribution
* use a real configurable floor instead of hardcoding `0.0`
* make sure specialist and non-specialist modes behave consistently
* add logs showing how many bars are removed by this floor

### Task C — Review specialist score formula

Evaluate whether the current specialist formula:

* over-compresses scores near zero
* relies too much on `p_side`
* fails when `mu_R` quality is weak

If needed, improve specialist scoring so that:

* head-agree bars rank meaningfully above disagree bars
* score magnitude has better separation
* threshold sweep has a usable right tail
* changes remain scale-aware and do not explode trade count blindly

Do not make random heuristic changes. Explain the reasoning.

### Task D — Convert binary blockers into measurable diagnostics first

Before changing gating behavior, instrument exactly how much each blocker removes:

* quality gate
* score hard-kills
* per-symbol threshold
* per-side threshold
* head disagreement
* cooldown
* DDT
* edge top-N
* any rolling ER / cap logic

Produce a funnel summary per fold and overall.

### Task E — Safer no-edge threshold behavior

For per-symbol/per-side thresholds:

* detect and loudly report any `inf` / HIGH_BAR states
* ensure the fallback behavior is explicit and logged
* prevent silent all-blocked specialist operation
* add unit or regression coverage for this path

### Task F — Fix any existing failing directional tests

Run targeted tests and fix drift/contract issues if they are real:

* `pytest tests/test_v5_directional.py -q`
* `pytest tests/test_v5_precision_audit.py -q`
* any focused specialist tests you add

Do not hide failures. Report root cause and exact code changes.

---

## Acceptance criteria

I will only consider this complete if your report includes:

1. **Root cause summary**

   * calibration issue vs code issue
   * exact contribution of specialist scoring, thresholding, and gates

2. **Concrete diffs**

   * file-by-file summary of changes

3. **Diagnostics evidence**

   * before/after score distribution for LONG and SHORT specialists
   * before/after selected thresholds
   * before/after trade counts
   * blocker funnel

4. **Test evidence**

   * exact pytest commands run
   * pass/fail results

5. **Safety check**

   * confirm no lookahead introduced
   * confirm no silent all-trades or zero-trades bug introduced
   * confirm specialist mode still respects direction correctly

---

## Output format

Return:

1. Root cause findings
2. Files changed
3. Exact code changes made
4. Test commands and results
5. Recommended follow-up command flags if thresholds are still too low

Be precise. Do not give me a motivational summary.
