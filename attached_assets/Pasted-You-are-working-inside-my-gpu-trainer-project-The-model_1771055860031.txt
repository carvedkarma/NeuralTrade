You are working inside my gpu_trainer project. The model trains fine during WARMUP (PR-AUC ~0.20) but collapses immediately at FULL stage: Pred1 jumps to 100%, mean_logit_pos ≈ mean_logit_neg, sep ~0.01. This is caused by a hard shock at the stage switch: pos_weight jumps (2.0 → ~4.7) while focal + OHEM are enabled at the same time. Fix this by making FULL-stage a gradual transition, not a step function.

GOALS
1) Prevent FULL-stage collapse (no Pred1=100% for multiple epochs, sep should not drop to ~0.01 instantly).
2) Keep training reproducible (save new schedule params into checkpoint config).
3) Keep backward compatibility with existing CLI.

CHANGES TO IMPLEMENT (DO ALL)

A) Add a “staged transition schedule” after warmup:
- warmup_epochs = existing (default 10)
- transition_epochs = NEW (default 15)
- After warmup, do a TRANSITION phase for transition_epochs where:
  - pos_weight ramps linearly from warmup_pos_weight (default 2.0) → final_pos_weight (computed from label rate, e.g. 4.73)
  - focal is enabled BUT with gamma ramp: start gamma=0.0 at transition start and ramp to gamma_final (default 0.5) by end of transition
  - alpha stays as configured (e.g. 0.45)
  - OHEM stays OFF during transition
- After transition ends, enter FULL phase:
  - focal gamma = gamma_final (default 0.5)
  - OHEM ON (neg_pct stays default 0.15)
  - pos_weight = final_pos_weight

B) Add LR handling at the stage switch:
- At the moment warmup → transition starts, reduce LR by 35% instantly (multiply current LR by 0.65) OR restart cosine schedule with a lower base lr (choose the simplest robust approach in your codebase).
- Do NOT break existing scheduler logic; integrate cleanly.

C) Add collapse guards + automatic fallback:
- During validation each epoch you already log Pred1, sep, PR-AUC.
- Add a guard: if (Pred1 > 98% for 2 consecutive epochs) OR (sep < 0.02 AND Pred1 > 95%), then:
  - automatically disable OHEM for next 5 epochs AND clamp pos_weight back to min(current_pos_weight, 2.5)
  - log a clear warning: “[COLLAPSE_GUARD] triggered…”
This makes training robust even if params are bad.

D) CLI args (backward compatible):
Add new args in quick_start.py (or wherever CLI lives):
- --transition-epochs (default 15)
- --focal-gamma-final (default 0.5)  (note: previously gamma=1.0 was used; now default 0.5)
- --lr-drop-on-transition (default 0.65)
- --disable-ohem-during-transition (default True)
- --collapse-guard (default True)
- --collapse-guard-pred1 (default 0.98)
- --collapse-guard-sep (default 0.02)
- --collapse-guard-freeze-epochs (default 5)
Save all these settings into checkpoint metadata/config.

E) Update logging so we can verify:
Each epoch log:
- stage (WARMUP/TRANSITION/FULL)
- current_pos_weight
- current_focal_gamma
- ohem_enabled
- lr_current
Also keep existing PR-AUC, Pred1, sep logs.

FILES TO TOUCH (likely)
- training loop / loss config module where stage is decided (LOSS_STAGE logic)
- quick_start.py argparse + wiring into trainer
- config saving / checkpoint metadata

ACCEPTANCE TESTS
Run a short training (e.g. 30 epochs) and show logs:
- During transition and FULL, Pred1 must NOT snap to 100% immediately.
- sep should not collapse to ~0.01 at stage switch; it should stay similar or improve vs warmup.
- PR-AUC should not drop sharply at stage switch (small noise ok).
- Ensure training still runs with old args (no new args required).

Deliver:
1) Commit-style summary of changes
2) List of files edited + key code locations
3) Sample logs from a 30-epoch run showing stage transition behaving correctly
