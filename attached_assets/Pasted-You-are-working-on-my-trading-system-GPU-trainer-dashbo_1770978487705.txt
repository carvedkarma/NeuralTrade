You are working on my trading system (GPU trainer + dashboard). Do a deep implementation audit and APPLY FIXES where needed.

GOALS
1) Fix Open Interest (OI) integration for multi-asset training correctly.
2) Ensure "paper trading" is not silently enabled and trades are not recorded unless explicitly allowed.
3) Add a --verify-system mode that proves the plumbing is correct with hard assertions + required logs.
4) Make sure the codebase version matches the runtime logs (no “features claimed but not actually active”).

TASK A — OI FIX (must be correct + multi-asset safe)
- In gpu_trainer/quick_start.py implement OI v2:
  - Default period MUST be 15m (match candle TF) with limit ~2880 (30 days).
  - Implement pagination over the 30-day window if endpoint caps at 500/req.
  - Cache MUST be per symbol: data_cache/open_interest_hist_{SYMBOL}.parquet
  - Cache quality checks:
    - stale > 24h -> refetch
    - require > 100 non-zero OI records in-window; otherwise warn and disable OI features for that symbol
  - In compute_oi_features():
    - drop/filter zero OI rows before computing delta/zscore
    - merge_asof MUST use tolerance = 2x period to avoid stale fills
    - coverage must be measured ONLY inside the OI 30-day window, not full candle history
    - alignment check must sample rows from within the OI window and print non-zero OI, Delta1h, Z30d

Required logs:
[OI_FETCH] symbol=... period=15m window=30d fetched_records=...
[OI_CACHE] symbol=... path=... stale=... nonzero=...
[OI_ALIGN] symbol=... coverage_in_window=...% tolerance=...

TASK B — PAPER TRADING / TRADE RECORDING GATING
- In gpu_trainer/live_runner.py:
  - Make paper default FALSE unless user passes --paper
  - Add a separate flag --record-trades (default FALSE). If false:
    - do NOT POST /api/live/trade
    - do NOT create local positions list that later resolves into “fake trades”
    - still push cycle logs/predictions
  - If --paper is true, allow recording trades as paper.
  - If --live is true, allow real execution (if implemented).
  - Add explicit log on startup:
    [MODE] record_trades=... paper=... live=...

TASK C — VERIFY MODE
- Add --verify-system flag in quick_start.py that runs a 30-cycle dry-run on 2 symbols and performs assertions:
  - HTF warmup gate works (WARMUP when bars insufficient)
  - no lookahead: HTF leakage sanity check prints PASS
  - if record_trades=false => trade endpoint is never called (assert)
  - OI: coverage_in_window is sensible (>80% if fetch succeeded) and alignment rows show non-zero values
  - PR-AUC pack wiring check: if flags are enabled, confirm the actual loss functions are active (assert by code path + printed config)
- Output verify_report.md with PASS/FAIL per section.

TASK D — VERSION CONSISTENCY
- Add a single version string constant and print it in all entry points.
- If runtime logs claim PR_AUC_PACK but code doesn’t implement it, STOP and fix wiring so logs match reality.

DELIVERABLES
- PR with changes
- verify_report.md
- Exact commands to run:
  1) verify mode
  2) training run with OI enabled
  3) live runner with record_trades disabled