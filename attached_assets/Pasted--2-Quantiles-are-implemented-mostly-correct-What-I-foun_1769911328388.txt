✅ (2) Quantiles are implemented (mostly correct)
What I found

gpu_trainer/models/multihead.py contains a QuantileHead that outputs q10, q25, q50, q75, q90 with monotonic constraints.

gpu_trainer/training/multihead_loss.py contains pinball loss support.

Status

✅ Quantile prediction head exists
✅ Pinball loss exists
⚠️ But your UI still shows a “triangle cone” because you are not generating quantile paths (you’re likely drawing a generic fan from sigma/uncertainty).

🟡 (1) “Return + volatility state heads” is only half implemented
What’s implemented

You already have a return mean head and uncertainty head:

RegressionHead outputs (mu, log_sigma) when configured

GaussianNLLLoss exists in training/multihead_loss.py

✅ return_mu / log_sigma concept exists

What’s missing vs your spec

Your spec requires:

forward log-return target

forward realized volatility target

vol_state label (3-class)

vol_state_logits head

vol-state cross-entropy loss

What I found instead:

In data/regression_targets.py, forward return is still computed as percent return:

returns = (future_prices - prices) / prices

There is realized volatility, but:

compute_forward_volatility() uses annualization = np.sqrt(288) (that’s 5m scaling, not 15m)

There is no vol_state label generation and no vol_state head in models/multihead.py.

Status

🟡 Return distribution head exists
❌ Volatility state head/logits do not exist
❌ Vol_state targets do not exist
⚠️ Forward vol annualization is wrong for 15m in one function
⚠️ mu target is pct-return, not log-return

❌ (4) Acceleration head is NOT implemented

Your spec requires:

a target like y_accel = mom_fwd - mom_now

a model head accel_pred

a loss term for accel

What I found:

No accel_pred / acceleration head in models/multihead.py

No acceleration target in data/regression_targets.py

No accel loss in training/multihead_loss.py

Status

❌ Not implemented

❌ (2C + UI) Quantile path projection is NOT implemented

Your spec requires:

create 16-step paths for q10/q50/q90 using alpha shaping

return arrays path_q10[], path_q50[], path_q90[] in API response

UI draws those paths (not a triangle)

What I found:

api/server.py does mention “quantiles for SL/TP derivation”

but there is no path_q10/path_q50/path_q90 in API output

no alpha-shaping path generator present

no forecast_mode flag in API output

Status

❌ Not implemented

❌ (3) Volatility gate “NO_FORECAST in chop” is NOT implemented

Your spec requires:

compute spread = q75 - q25

gate: if contraction OR spread < 3*cost → NO_FORECAST + HOLD

UI hides cone when NO_FORECAST

What I found:

api/server.py does not contain:

forecast_mode

NO_FORECAST

FLOW_FORECAST

no gating logic based on vol_state (since vol_state doesn’t exist yet)

no gating logic based on q75-q25 as a forecast suppression rule (it is used for SL/TP in some places, but not to hide projections)

Status

❌ Not implemented

Bottom line
What’s correctly implemented already

✅ Quantile head (q10/q25/q50/q75/q90)
✅ Gaussian NLL loss for (mu, log_sigma) exists
✅ Multihead prediction mentions quantiles in API

What is NOT implemented (still missing)

❌ vol_state targets + vol_state head + vol_state loss
❌ acceleration targets + accel head + accel loss
❌ quantile path projection arrays in API
❌ forecast_mode gating + UI suppression (NO_FORECAST)

One extra important bug I noticed (15m scaling)

In data/regression_targets.py:

compute_realized_volatility() uses bars_per_day=96 default (✅ correct for 15m)

but compute_forward_volatility() uses annualization = np.sqrt(288) (❌ that’s 5m)
So forward sigma may be mis-scaled for 15m, which affects confidence and gating.