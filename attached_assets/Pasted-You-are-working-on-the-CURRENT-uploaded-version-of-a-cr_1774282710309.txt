You are working on the CURRENT uploaded version of a crypto trading repo. Fix only the remaining high-impact issues that are still causing trust and test failures. Do not do broad refactors. Do not invent new strategy ideas. Make the codebase internally consistent, backward-compatible where practical, and safer to validate tonight.

IMPORTANT RULES
- Work from the CURRENT code state only.
- Preserve existing CLI behavior unless absolutely necessary.
- Prefer compatibility shims over breaking API changes.
- Add comments where behavior changed.
- After each task, report exact files changed and before/after evidence.
- Run targeted tests after each major fix.

TARGET TEST COMMAND
pytest -q tests/test_v5_directional.py tests/test_v5_precision_audit.py tests/test_side_conditional.py tests/test_v5_features.py tests/test_correlation.py tests/test_drawdown_throttle.py tests/test_v5_position_sizer.py

PRIORITY ORDER
1. Fix compute_v5_scores correctness
2. Restore _run_v5_sweep backward compatibility
3. Fix v5_quality_mask ref_arrays behavior
4. Restore backward-compatible FeatureEngineer API
5. Restore backward-compatible V5ForecasterConfig API
6. Make correlation contract compatible with current design and tests
7. Validate adaptive sizing behavior and align tests or code intentionally

==================================================
TASK 1 — FIX compute_v5_scores CORRECTNESS
==================================================

Problem:
Current tests indicate scoring is still wrong in decision-critical ways:
- score invariance fails when mu/risk is held constant
- side selection is wrong in some action_head cases
- score can remain positive below intended breakeven threshold

What to do:
- Inspect compute_v5_scores in train/v5_train.py
- Preserve current architecture where possible
- Make score depend on risk-normalized edge consistently
- For side_mode='action_head':
  - choose side from p_long vs p_short cleanly
  - compute p_side from the chosen side
- Ensure score turns negative or non-positive when p_side is below breakeven implied by score_lambda
- Preserve min_p_side, min_p_short, min_mu_r_score, slippage handling, and side-aware logic unless clearly wrong
- Avoid introducing extra complexity

Acceptance criteria:
- tests in tests/test_v5_directional.py pass
- add comments documenting the final formula and breakeven interpretation
- print or log small diagnostics only if already consistent with repo style

==================================================
TASK 2 — RESTORE _run_v5_sweep BACKWARD COMPATIBILITY
==================================================

Problem:
Some callers/tests still expect a legacy 4-value return, but current code returns more values.

What to do:
- Update _run_v5_sweep in train/v5_train.py
- Add a backward-compatible mode, e.g.:
  return_full=False by default
- If return_full=False:
  return legacy 4-tuple expected by old callers/tests
- If return_full=True:
  return the expanded metrics currently used by newer code
- Update internal callers that need expanded metrics to request return_full=True explicitly

Acceptance criteria:
- side-conditional and related unpacking tests pass
- no silent behavior break for newer internal users

==================================================
TASK 3 — FIX v5_quality_mask ref_arrays BEHAVIOR
==================================================

Problem:
ref_arrays path is still not behaving correctly. Anti-lookahead tests indicate thresholds are not truly anchored to reference arrays when they should be.

What to do:
- Inspect v5_quality_mask in train/v5_train.py
- When ref_arrays is provided:
  - derive adaptive thresholds from ref_arrays only
  - do not mix current arrays into threshold estimation
- Use current arrays only for evaluating which bars pass/fail
- Ensure every adaptive threshold source is consistent, including sigma or similar fallback fields
- Add a small diagnostic field like threshold_source if the function already returns diagnostics

Acceptance criteria:
- ref_arrays-related tests in tests/test_v5_precision_audit.py pass
- threshold estimation source is explicit and consistent

==================================================
TASK 4 — RESTORE BACKWARD-COMPATIBLE FeatureEngineer API
==================================================

Problem:
tests expect FeatureEngineer(symbols=[...], timeframes=[...]) but current implementation signature is no longer compatible.

What to do:
- Inspect data/pipeline.py
- Restore compatibility so FeatureEngineer can be constructed with legacy kwargs:
  - symbols
  - timeframes
- If those fields are no longer used internally, accept and store/ignore them safely without breaking
- Preserve current newer constructor behavior too

Acceptance criteria:
- tests/test_v5_features.py constructor-related failures pass
- no break to current code paths

==================================================
TASK 5 — RESTORE BACKWARD-COMPATIBLE V5ForecasterConfig API
==================================================

Problem:
tests expect V5ForecasterConfig(n_features=...) but current config uses input_dim or another renamed field.

What to do:
- Inspect the config/model definitions
- Restore compatibility so both work:
  - n_features
  - input_dim
- Normalize internally to one canonical field
- Add validation so contradictory values raise a clear error rather than silently choosing one

Acceptance criteria:
- model/config compatibility tests pass
- old and new constructor styles both work

==================================================
TASK 6 — MAKE CORRELATION CONTRACT COMPATIBLE WITH CURRENT DESIGN
==================================================

Problem:
Code has shifted from hard blocking to size penalty, but tests or callers still expect blocker-style behavior.

What to do:
- Inspect train/v5_correlation.py and any callers
- Keep the newer penalty-oriented design if that is now the intended architecture
- But restore compatibility for blocker-oriented tests/callers where practical

Recommended approach:
- allow should_block(...) to return blocker behavior when explicitly requested or when threshold/reliability logic clearly indicates an old-style block
- otherwise expose the new penalty path cleanly
- document the intended contract in comments

Acceptance criteria:
- tests/test_correlation.py pass
- current reporting/penalty logic remains usable
- no ambiguous behavior

==================================================
TASK 7 — VALIDATE AND INTENTIONALLY ALIGN ADAPTIVE SIZING
==================================================

Problem:
Sizing was changed to address floor-pinning, but current behavior and tests are not fully aligned. Need explicit confirmation of intended semantics.

What to do:
- Inspect train/v5_position_sizer.py
- Trace final_size_mult behavior for low, medium, high conviction cases
- Decide intentionally whether low-p_win / weak-edge setups should:
  - stay at min_size_mult
  - or rise modestly above min if percentile/quality is strong
- Then align code and/or tests to the intended conservative behavior
- Do NOT make sizing more aggressive than necessary

Diagnostics to add:
- min/mean/max/std final_size_mult in any existing summary path if easy
- percent of trades above floor if easy

Acceptance criteria:
- tests/test_v5_position_sizer.py pass
- behavior is documented in code comments
- sizing remains conservative

==================================================
VALIDATION / DELIVERABLES
==================================================

After implementation:
1. Run the target test command
2. Report:
   - exact before/after pass/fail counts
   - exact files changed
   - summary of each fix
3. Call out any remaining failures honestly
4. Do not claim live-trading readiness unless all core decision-path issues above are actually resolved

Do the work now, not just the plan.