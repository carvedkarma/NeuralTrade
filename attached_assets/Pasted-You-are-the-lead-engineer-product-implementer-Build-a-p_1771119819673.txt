You are the lead engineer + product implementer. Build a production-ready, web-only, multi-tenant (multi-state AU) platform called “Swyft Rentals” with separate subdomain apps:

- swyftrentals.com.au (marketing/legal/entry selector only)
- rent.swyftrentals.com.au (rentals booking app)
- ride.swyftrentals.com.au (premium ride booking app)
- drivers.swyftrentals.com.au (driver portal: opportunities + accept + upcoming bookings)
- admin.swyftrentals.com.au (ops/admin portal)
- api.swyftrentals.com.au (backend API)
- auth.swyftrentals.com.au (central auth / SSO)

GOAL: Finalize a complete system end-to-end. No “TODO”, no mock endpoints, no sample copy. Everything must function.

TECH STACK (LOCKED):
- Monorepo (pnpm workspaces) with Next.js (App Router) for all frontends
- UI: shadcn/ui + Tailwind + Framer Motion (for animations)
- Backend: NestJS (TypeScript) + Prisma + PostgreSQL
- Realtime: WebSocket gateway in NestJS
- Queue: BullMQ + Redis (for notifications, dispatch timers, analytics events)
- Storage: S3-compatible (use local MinIO in dev)
- Payments: Stripe (stub keys in env, but implement actual flows with Stripe SDK)
- Maps: Google Maps (use env key; implement address autocomplete abstraction)

ARCHITECTURE:
- Shared design system package: @swyft/ui (tokens + components)
- Shared types package: @swyft/types
- Shared auth package: @swyft/auth-client (SSO helpers)
- Frontend apps: apps/marketing, apps/rent, apps/ride, apps/drivers, apps/admin, apps/auth
- Backend: apps/api
- Infra: docker-compose for postgres, redis, minio

DOMAIN / SSO REQUIREMENTS:
- Implement central auth at auth.swyftrentals.com.au
- Session cookie scoped to .swyftrentals.com.au to share login across subdomains
- Roles: CUSTOMER, DRIVER, STAFF, ADMIN
- RBAC with scope: GLOBAL, STATE, BRANCH
- MFA option for ADMIN/STAFF (TOTP)

DESIGN + ANIMATIONS (MUST IMPLEMENT):
Theme “Precision Luxury Mobility”:
- Base: Midnight Slate/Graphite cards, Eucalyptus Teal accent
- Global motion rules: 200–350ms spring transitions; never > 500ms
- Signature animations:
  1) Lift & Glow hover on cards/buttons
  2) Morph Price: when dates/inputs change, prices animate with count-up + fade/slide
  3) Slide Reveal: panels expand instead of modals where possible
  4) State Capsule Animation: booking statuses animate through states
  5) Opportunity Pulse: new driver jobs softly pulse + countdown ring

PAGES / UX (NO SAMPLES, REAL WORKFLOWS):

1) Marketing main domain (apps/marketing):
- Home with two large selectable cards: Rent a Car / Book a Premium Ride
- About, Locations/Service Areas (multi-state list), Policies index, Contact
- No booking here; only deep links to rent/ride.

2) Rentals app (apps/rent):
- Booking Ribbon (sticky): pickup branch, return branch toggle, pickup datetime, return datetime, driver age, search CTA
- Results: filter left, bento vehicle cards right
- Vehicle detail: gallery, specs, inclusions, deposit/excess, add-ons, sticky checkout summary
- Checkout steps: driver details + licence upload, add-ons & insurance/excess reduction, payment & e-sign agreement
- Customer account: bookings, invoices, profile, saved addresses

3) Ride app (apps/ride):
- Quote Builder split layout: ride type (airport/point-to-point/hourly), pickup/dropoff, schedule, passengers/luggage, add-ons
- Tier cards: Executive/Luxury/Premium SUV/People Mover (visual tier styling)
- Confirm booking; booking enters dispatch pool
- Booking tracking page: status capsule, driver accepted state, live updates via websocket

4) Driver portal (apps/drivers):
- Driver onboarding: profile, docs upload, tier approval status, vehicle assignment
- Opportunities page: 3-pane desktop layout (feed / map / detail), mobile tabs
- Open pool jobs visible based on driver tier + vehicle eligibility + service zone + no schedule conflict
- Driver can ACCEPT job (race-safe lock). If already taken, show “Job taken”.
- My Jobs: upcoming list + calendar; status updates (en route/arrived/onboard/completed)
- Earnings summary page (basic v1): completed jobs + totals

5) Admin portal (apps/admin):
- Full RBAC + audit logs (before/after snapshots) for sensitive actions
- Modules:
  - Users & Roles (role builder, permission matrix, scoped assignment)
  - Vehicle Catalog (make/model/trim import + browse)
  - Fleet Units (actual vehicles: rego/VIN/status/branch/state)
  - Mapping UI: assign vehicles/models to Rental Categories + Ride Tiers
  - Rentals Bookings management (modify, swap, extend, refunds, agreements)
  - Rides Bookings & Dispatch oversight (job pool visibility, acceptance history)
  - Pricing & Rules engine (state/branch/category, seasonal multipliers, after-hours, deposits/excess, add-ons)
  - Driver management (docs expiry, tier approvals, performance stats)
  - Finance (invoices, refunds, payouts exports)
  - Analytics/Stats:
    * Business stats: customers/drivers, bookings, revenue, utilization, cancellations
    * Traffic stats integration: choose Plausible by default; add adapter for GA4 later
    * Conversion funnel events from internal event tracking
  - Support tools: booking search, notes, message templates
- Charts: use Recharts, clean minimal style

CORE BACKEND FEATURES (apps/api):
- Prisma schema with these core entities:
  State, Branch, ServiceZone,
  VehicleMake, VehicleModel, VehicleTrim (catalog),
  Vehicle (fleet unit), RentalCategory, RideTier,
  VehicleCategoryMap, VehicleTierMap,
  User, Role, Permission, RolePermission, UserRole (scoped),
  DriverProfile, DriverVehicle, DriverTierApproval, DriverSchedule,
  RentalBooking, RentalAddOn, RentalAgreement, RentalInspection,
  RideBooking, RideQuote, RideDispatchPoolItem, RideAcceptance, RideStatusEvent,
  Payment, Invoice, Refund,
  AuditLog,
  AnalyticsEvent
- Rules Engine:
  - state + branch aware pricing and eligibility rules
  - rental deposits/excess/age bands
  - ride fare rules per zone/time band + surcharges
- Dispatch pool:
  - ride bookings create pool items
  - eligible drivers query pool
  - accept uses transaction + unique constraint to prevent double accept
  - offer expiry handled by queue job
- Realtime:
  - websockets for booking status updates to customer + driver + admin
- Location Search:
  - abstraction layer:
    * rentals: branch-first suggestions
    * rides: address-first with autocomplete
  - implement with Google Places if key exists; fallback to simple server-side search in dev

PAYMENTS:
- Stripe integration:
  - Rentals: pre-auth intent + capture on completion
  - Rides: charge at booking or after completion (configurable)
- Store payment state in DB, expose webhooks endpoint, secure signature verification

ANALYTICS:
- Internal event tracking:
  rental_search, vehicle_view, checkout_start, payment_success
  ride_quote, ride_request, job_posted, job_accepted, ride_complete
- Admin analytics pages read from AnalyticsEvent + aggregated tables
- Traffic analytics: integrate Plausible script on marketing/rent/ride; show aggregated numbers in admin via Plausible API adapter (use env token)

SECURITY + COMPLIANCE:
- Audit everything sensitive: role changes, pricing changes, refunds, driver approvals, vehicle status changes
- PII handling: encrypt sensitive fields at rest (server-side)
- Rate-limit auth endpoints
- CSRF protection for session-based auth
- Input validation with zod/class-validator
- Logging with correlation IDs

DELIVERABLES:
1) Working monorepo with docker-compose for postgres+redis+minio
2) Seed scripts to create:
   - default states, example branches (ONLY minimal structural data, no marketing sample text)
   - admin user (from env) and basic roles
3) End-to-end flows verified:
   - customer books rental and sees in account
   - customer books ride, drivers see as opportunity, one accepts, status updates flow
   - admin can manage vehicles, roles, pricing, see stats
4) Deployment readiness:
   - environment variable templates (.env.example)
   - production build scripts
   - database migrations

WORK PLAN (MANDATORY EXECUTION):
- Step 1: Scaffold monorepo structure + shared packages
- Step 2: Implement auth SSO + RBAC + admin protected routes
- Step 3: Implement core schema + migrations + seed
- Step 4: Implement rentals flow end-to-end (search -> checkout -> booking -> account)
- Step 5: Implement rides flow end-to-end (quote -> booking -> dispatch pool -> driver accept -> tracking)
- Step 6: Implement admin modules (vehicles, mapping, pricing, bookings, roles, audit logs)
- Step 7: Implement analytics module (internal events + plausible adapter)
- Step 8: Polish UI/animations across all pages with the specified motion system
- Step 9: Add tests for critical race conditions (driver accept) + webhook signature verification

QUALITY BAR:
- No placeholder “lorem ipsum” content
- No broken routes
- No hardcoded secrets
- All forms validated
- Fast UX, mobile responsive
- Clean, premium futuristic theme consistent everywhere

Start now and deliver the complete codebase in one repo.
