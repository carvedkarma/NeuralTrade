You are my Replit Agent. Implement a 24/7 multi-asset live system that:
- Runs continuous inference every 15m candle close for BTCUSDT, ETHUSDT, SOLUSDT, BNBUSDT
- Takes trades (paper by default) and pushes predictions + trades to my dashboard
- Collects new candles continuously and performs SAFE scheduled learning (daily retrain) with model promotion only if improved

STRICT REQUIREMENTS
1) Do NOT do naive online learning every candle close (risk of collapse).
   Instead:
   - Continuous inference/trading loop every 15m close
   - Append new data to dataset cache
   - Retrain on schedule (daily at configurable time or every N hours)
   - Promote new model only if it beats current model on robust evaluation.

2) Preserve parity everywhere:
   - Same feature pipeline for training + inference (STF47 + HTF10 + Funding3 + OI3 = 63)
   - Same triple_barrier.py for labeling and evaluation
   - Same cost model (fees/spread/slip_k)
   - Same geometry/best_policy JSON is used by live trading

3) Multi-asset architecture:
   - Maintain per-symbol state: last candle timestamp processed, cooldown counter, open positions, last decision
   - Portfolio constraints: 10% total risk cap, 5% per symbol, correlation block (BTC/ETH same direction), cooldown enforcement
   - LTF execution module used for entries (1m/3m/5m pullback + VWAP/EMA confirmation); allow a flag --allow-market-fallback.

4) Dashboard:
   - Every 15m cycle, push per-symbol:
     prediction payload: time, price, p_enter, HTF trend signs, slope_ok, range_ok, direction, threshold used, decision ENTER/HOLD, reasons
   - On trade open/close, push trade record:
     symbol, side, entry_time, entry_price, sl, tp, size, costs, outcome (TP/SL/EXP), realized_R_gross/net/sized
   - Add a new endpoint or DB table for “learning_stats” per asset:
     model_version, trained_until_ts, training_samples, val_PR_AUC, best_policy, regime_eval summary, PF_net, E_net, trades/day, profitable_regimes

IMPLEMENTATION TASKS
A) Live 24/7 Runner
- Add command: `python quick_start.py --url URL --live --symbols BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT`
- Loop forever:
  - Wait until next 15m close (use exchange time + sleep)
  - For each symbol:
    - fetch latest 15m candles (enough lookback for features + labeling horizon)
    - compute features (63) with strict column validation and version match
    - run model inference (single-row, shape [1,63])
    - apply HTF gates + policy + portfolio filter + LTF execution decision
    - push dashboard update (prediction + decision)
  - Ensure robust error handling: retries, rate limit backoff, and never crash the loop

B) Data Cache
- Maintain per-symbol data cache under data_cache/{symbol}/candles_15m.parquet
- Append new closed candles only (dedupe by timestamp)
- Keep at least last 5 years if available (configurable)
- Log coverage warnings for funding/OI sparse data

C) Scheduled Retrain + Safe Promotion (learning)
- Add flags:
  --retrain-every-hours 24 (default daily)
  --retrain-min-new-bars 96 (default 1 day of 15m bars)
  --promotion-min-pf-net 1.10
  --promotion-min-profitable-regimes 2
  --promotion-target-tpd 2.5 --promotion-tpd-tol 1.5
- Retrain pipeline per symbol (or shared model, but start per symbol for simplicity):
  1) Build dataset using cached candles up to latest timestamp
  2) Label with triple barrier + HTF alignment + r_min_expiry (same config used in live)
  3) Train enter_quality model with BCE pos_weight, warmup+cosine schedule, patience
  4) Run `--regime-eval` WITH costs realism (fees/spread/slip_k) and target TPD
  5) Run `--geometry-sweep` to select best policy for that model
  6) Compare candidate model vs currently deployed model:
     - PF_net, E_net, net-profitable regimes count, TPD within band
  7) If candidate passes thresholds and beats deployed, promote:
     - move checkpoint to checkpoints/deployed/{symbol}/
     - update deployed metadata JSON
     - push “learning_stats” update to dashboard
  8) Otherwise keep current model and log “no promotion”

D) Learning Stats + “Is it improving?”
- Compute rolling live performance per symbol:
  - last 7d PF_net/E_net/TPD vs previous 7d
  - show “Improving”, “Flat”, or “Worse” with a confidence note (sample size)
- Push these to dashboard every day after retrain (and optionally every hour)

E) Versioning
- Bump feature_version to v3.5.0_live_learning_loop (or next)
- Save per-symbol:
  - model checkpoint
  - scaler
  - feature_columns list
  - feature_version
  - training config + barrier config + cost config + best_policy config
- Hard fail if feature columns mismatch at inference/training.

DELIVERABLES
1) Working 24/7 live loop (paper mode default) with dashboard updates
2) Scheduled retrain + safe promotion system
3) Per-asset learning stats dashboard feed
4) Strong logging, no lookahead, no silent failures

Make the changes directly in the repo with clean, readable code and CLI help text.
