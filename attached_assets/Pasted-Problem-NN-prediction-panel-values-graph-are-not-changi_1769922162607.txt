Problem: NN prediction panel values/graph are not changing after refresh.
Need to verify if we are (A) not calling API, (B) caching, (C) using identical candle inputs (no new closed 15m bar), or (D) UI state not updating.

Implement these diagnostics + fixes:

1) UI: Confirm refresh triggers a real request
- In the Refresh Prediction button handler:
  - log to console "refresh clicked" + Date.now()
  - force-fetch with cache disabled:
      fetch(url, { cache: "no-store" })
- In Network tab we must see a new request every click.

2) API response: add tracing fields (always)
Add these fields to /api/gpu/nn-prediction (and /predict/stf if used):
- prediction_id: uuid
- server_ts: ISO timestamp
- last_closed_candle_ts: ISO timestamp of the last CLOSED 15m candle used
- window_start_ts / window_end_ts (first/last candle in input window)
- input_hash: sha1 of the candle OHLCV array used for features
- output_hash: sha1 of key outputs (q10,q50,q90,signal,forecast_mode)
- cache_hit: boolean
- model_run_ms: inference time

3) Backend: fix caching to be candle-aware
If any caching exists, change cache key to include last_closed_candle_ts:
cache_key = f"{symbol}:{tf}:{horizon}:{last_closed_candle_ts}"
OR disable caching entirely for debugging.
Never cache across candle closes.

4) Data freshness: enforce closed-candle logic
Ensure we only use completed 15m candles (no leakage).
If user refreshes before new candle close:
- return same input_hash and add message: "No new closed candle yet; prediction unchanged."

5) UI display: show debug line
Under the prediction panel, display:
server_ts | last_closed_candle_ts | input_hash short | cache_hit
This will explain to user why numbers don’t change.

Acceptance criteria:
- Each refresh creates a new request (new prediction_id + server_ts).
- If last_closed_candle_ts unchanged, input_hash unchanged and UI explains “no new closed candle”.
- If last_closed_candle_ts changed, input_hash changes and outputs should update.
- No stale “same payload” due to caching or UI memoization.
