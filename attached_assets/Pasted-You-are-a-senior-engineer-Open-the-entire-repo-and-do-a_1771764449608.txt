You are a senior engineer. Open the entire repo and do a full end-to-end audit of the training + forward-test pipeline (v5). Goal: fix the “0 trades / wrong Trades-per-day / multi-regime total=0 or total=7 / -inf score stats / ADX divide warnings” issues and add clear diagnostics.

DO THIS IN ORDER:

1) Reproduce
- Run a minimal forward test (single fold) with v5 + diagnostics and record the console output.
- Identify where bars are filtered out: candidates -> above threshold -> after cooldown -> after regime gate -> after ADX gate -> after DDT gate -> final taken trades.

2) Fix the “max trades per day counts bars not trades” bug
- Search for the variable/counter that enforces `--v5-max-trades-per-day`.
- Ensure the daily counter increments ONLY when a trade is actually OPENED/RECORDED (not when a bar is evaluated).
- If the code currently increments during candidate iteration or before gates, move it to the trade-open location.
- Add a unit test: bars evaluated >> trades taken; counter must reflect trades, not evaluated bars.

3) Fix score diagnostics producing `mean=-inf` or NaN percentiles
- Locate the log block that prints “Score stats mean=-inf … p50 … p90 … %pos”.
- Modify to compute all summary stats ONLY over finite values:
  - `mask = np.isfinite(scores)`
  - if `mask.sum()==0`, print “no finite scores” and skip percentiles.
- Add a test to ensure -inf/NaN scores do not break stats or threshold selection.

4) Fix ADX divide warnings and NaN propagation
- Find ADX/DI computation where RuntimeWarning occurs (“invalid value encountered in divide”).
- Add eps clamps for denominators (e.g., 1e-12) and force safe outputs:
  - if denom is 0, set DI/DX to 0 (not NaN).
  - ensure ADX is finite after warmup; if still NaN, set to 0.
- Add a test that ADX contains no NaNs for flat price / constant candles.

5) Multi-Regime classifier: prevent “total=0” or “total=7” coverage
- Find multi-regime code and its assignment logic.
- Ensure EVERY bar gets a regime label (never “unclassified” silently):
  - If it doesn’t match trending/choppy/high/low, assign “neutral” or “unknown” explicitly.
  - Do not allow regime classification gaps to block trading unless explicitly intended.
- Confirm the summary prints `total == number_of_bars_evaluated` (or very close after warmup), not single digits.
- Add a test that regime labels cover >95% of bars after warmup.

6) Decide and implement the intended behavior of multi-regime
- If multi-regime is meant to be SIZING-only: it must NOT veto trades, and it must NOT automatically disable EMA200 gate.
- If multi-regime is meant to be a GATE: implement explicit rules (e.g., block choppy/high_vol or raise threshold), but keep coverage complete.
- Make this behavior obvious in logs: “multi-regime gating: ON/OFF”.

7) Add drop-off diagnostics (must-have)
Add a single summary line per fold:
- bars_total
- candidates_found
- above_threshold
- after_cooldown
- after_regime
- after_adx
- after_ddt
- trades_taken
Also add per-gate “blocked count” counters.

8) Run full test suite
- Ensure all tests pass.
- Add/extend tests for the above bugs (TPD counter, finite scores, ADX stability, multi-regime coverage).

9) Deliverables
- Commit code changes.
- Provide a short CHANGELOG with the exact files modified and why.
- Provide the recommended one-line command for multi-regime + DDT targeting ~5–7 trades/day AFTER the fixes.