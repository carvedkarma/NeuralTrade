Focus only on changes that materially improve tonight's trading readiness and strategy behavior.

Do NOT rewrite compute_v5_scores if the existing math already matches:
mu_over_risk * ((1+lambda)*p_side - lambda)

Do NOT spend time on generic test-fix work unless a test is actually failing in the current repo.

Priority order:

1. FIX ADAPTIVE SIZING
- Trace the full size multiplier path from score -> scaling -> clamp -> final order size
- Diagnose why trades are pinned at 0.5x floor
- Print/log intermediate values:
  raw_score, normalized_score, regime_mult, dd_mult, corr_mult, final_size_mult
- Ensure high-conviction trades can actually size above floor
- Keep hard max cap, but make floor apply only when score is weak, not always

2. ALIGN TRAIN/LIVE CONFIG
- Create shared_v5_trade_config.py
- Centralize:
  score_threshold
  score_lambda
  slippage/costs
  corr_thresh
  cooldown
  symbol kill threshold
  sizing params
- Make train and live load from same defaults unless explicitly overridden

3. FIX SYMBOL KILL THRESHOLD LOGIC
- Audit symbol disable / recovery logic
- Log exact reasons a symbol is killed or re-enabled
- Revisit current thresholding so it does not permanently suppress symbols that can recover
- Make the threshold configurable from shared config

4. CORRELATION FILTER
- Do not over-engineer
- If current corr filter has little effect, either:
  a) disable hard blocking temporarily, or
  b) convert it to a mild size penalty only
- Add diagnostics:
  aligned_days, corr_value, reliability_flag, action_taken

5. OPTIONAL IMPORT SAFETY
- Wrap pywt and transformers imports in try/except
- Missing optional libs must not crash the repo

6. LIVE LOG TRUTHFULNESS
- If no real exchange adapter is wired, never log placing_order
- Replace with LIVE_SIGNAL_ONLY or PAPER_ENTRY style logs

7. MINIMAL SAFETY SWITCHES
- Add startup halt on stale market data
- Add halt on repeated API errors
- Add max daily loss block for new entries in live_runner

Deliver:
- exact files changed
- before/after behavior of sizing
- proof that train/live configs now match
- proof that optional missing deps no longer crash startup
- proof that live logs are truthful