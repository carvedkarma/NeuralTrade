REPLIT AGENT — IMPLEMENT “SMART TRADE MANAGER v4.4.0” (dynamic exits)

Goal: Add intelligence to CLOSE trades early (profit lock, breakeven, trailing, time exit).
Do NOT retrain model. Use existing lanes + HTF score + p_enter.

A) Add trade_manager.py
Create TradeManager class with:
- update_open_positions(symbol, position, latest_15m_bar, optional_3m_series)
- returns action: HOLD | MOVE_SL | TRAIL_SL | CLOSE_PARTIAL | CLOSE_FULL with reason

B) Mandatory exit rules (apply to ALL lanes)
1) TIME_EXIT:
- if bars_held >= lane_horizon => CLOSE_FULL reason=TIME_EXIT

2) BREAKEVEN:
- if max_favorable_r >= +0.35R and sl < entry => MOVE_SL to entry reason=BREAKEVEN

3) TRAIL:
- if max_favorable_r >= +0.60R:
  trail_distance_r = 0.40R
  new_sl = peak_price - trail_distance_r * risk_per_unit (LONG)
  only tighten SL, never loosen
  action=TRAIL_SL reason=TRAIL_LOCK

4) PROFIT_STALL EXIT:
- if unrealized_r >= +0.70R AND no new favorable extreme for 2 bars => CLOSE_FULL reason=STALL_TAKEPROFIT

5) ADVERSE FLIP CUT:
- if unrealized_r <= -0.60R AND htf_score dropped by >=2 OR p_enter < lane_threshold_used*0.8
  => CLOSE_FULL reason=ADVERSE_FLIP

C) Intrabar TP/SL correctness (paper mode)
When a trade is open:
- fetch 1m candles (last 60) OR use 15m high/low
- Resolve TP/SL using high/low not close:
  LONG: if low <= SL => SL hit; if high >= TP => TP hit
  SHORT symmetric
- If both hit in same bar: assume worst-case (SL first) unless using 1m data.

D) Wire into live_runner loop
Every cycle:
- for each open position: call TradeManager
- if action emits close or SL update: push to execution module + dashboard update with reason

E) Dashboard changes
Trade Journal: add columns:
- exit_reason
- max_favorable_r (MFE)
- max_adverse_r (MAE)
- time_exit (bool)
- breakeven_moved (bool)
Add “Trade Detail Popup” with:
- entry conditions, lane, thresholds, htf_score
- why closed (exit_reason + metrics)

F) Logging required
[TM] sym=BTC lane=FLOW uR=+0.72 mfe=+0.81 mae=-0.22 action=CLOSE_FULL reason=STALL_TAKEPROFIT
[TM_SL] sym=BTC action=BREAKEVEN new_sl=...
